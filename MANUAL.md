# VVV_Trade 系统说明书

> 快照：2026-08-06 00:33 UTC · 代码 16,991 行 Python + 2,586 行前端 · 数据库 230 MiB / 19 张业务表 · 302 个测试
> 版本：`RULES_VERSION v3.1` · `AUDIT_VERSION a8` · `LEVELS lv1` / `LOCATION loc1` / `STOPCHECK stop1` / `VOLNOTE vol1`
>
> **本文是"这个系统是什么、怎么用、每部分为什么这么设计"**。
> 历史快照与审计见 `SYSTEM_LOG_20260731.md`（v1）与 `SYSTEM_LOG_20260805.md`（波动率栈时代）；
> 缺口清单见 `docs/GAPS_20260805.md`。

---

## 0. 三句话

1. **它回答两个问题**：*现在是什么市场环境* → *在这个环境该做什么*。
2. **它不预测方向**。它在关键位置等关键信号，用算得出的风险做反应。大多数时刻它会告诉你"等"——这是特征，不是故障。
3. **它不下单**。所有输出止于"建议关注 + 完整理由"，执行由你。

**边界声明**：这是研究与决策辅助工具，不构成投资建议；它的阈值目前是**版本化的先验值，尚未经回测校准**。

---

## 1. 快速上手

### 启动

```bash
cd /Users/luoyingdong/Documents/VVV_Trade && nohup .venv/bin/python collector.py > /dev/null 2>&1 &
```

```bash
cd /Users/luoyingdong/Documents/VVV_Trade && nohup .venv/bin/python dashboard.py > /dev/null 2>&1 &
```

先采集、后面板。美股 IV 相关管线还需要 **moomoo OpenD 已登录**（图形版在 `~/Applications/moomoo_OpenD.app`）。

### 看什么

| 打开 | 看到 |
|---|---|
| `http://127.0.0.1:8787/` | **总览**：74 个品种里，今天哪几个值得看 |
| `http://127.0.0.1:8787/symbol?symbol=BTC-USDT` | **详情页**：这一个品种的完整判断链与全部依据 |
| 两页右上角 `VVVhermes` | 助手：总览页问横截面，详情页问单品种 |

### 一分钟自检

看总览页顶栏的**心跳条**：五个点（近端IV / 个股IV / 期限曲线 / 衍生品 / OpenD）。**没有红点就是一切正常**；灰点表示"盘外不采"，不是故障。

---

## 2. 系统全景

```
数据源                采集              判断                     出口
─────────────────────────────────────────────────────────────────────
Deribit    ┐                    ┌─ ① 定状态（4h regime）
Binance    │                    │
 fapi/eapi ├─ collector.py ─┐   ├─ ② 定位置（4h 关键位区间）      总览页
moomoo     │   每 300 秒    ├─ SQLite ─┤                          详情页
CBOE       │   13 类 sync   │   19 表  ├─ ③ 找共振（4h 主导+1d/1h） VVVhermes
Yahoo      ┘                └   230MiB └─ ④ 风控与警告
                                          （止损宽度 · IV · 事件）
```

**两个进程，一个库**：`collector` 只写、`dashboard` 只读（唯一例外是对话表），靠 SQLite WAL 解耦。任一侧崩溃另一侧照常工作。

---

## 3. 四层判断流程（核心章节）

这是系统的主干。每一层只做一件事，层与层之间的接口是明确的数据结构。

### ① 定状态 —— 4h regime

五个状态，由一棵**四层短路树**判定（顺序即优先级）：

| 状态 | 判据 | 中文标签 |
|---|---|---|
| `trend_up` / `trend_down` | `er_rank ≥ 0.60` 且 `\|direction\| ≥ 0.30` | 趋势上行 / 趋势下行 |
| `squeeze` | `bbw_rank < 0.15` 且 `atr_rank < 0.30` | 低波动挤压（蓄势） |
| `high_vol_chop` | `atr_rank > 0.85` | 高波动非趋势（趋势条件未齐） |
| `range` | 以上都不满足 | 震荡 |

**五个阈值决定状态**（`0.60 / 0.30 / 0.15 / 0.30 / 0.85`）；第六个 `tilt_confirm = 0.10` **只调置信度、不改状态**。

**为什么标签这么啰嗦**：`high_vol_chop` 的规则只证明了"ATR 分位高且没过趋势判定"，它**没有**证明"无序"，也**没有**证明"方向未定"（实测 35.45% 的该态行方向腿其实已经通过）。所以标签只说"趋势条件未齐"——这是规则字面上唯一成立的陈述。系统里所有标签都遵循这个原则：**只说规则真正检验过的东西**。

**确认层**：状态切换要经过非对称迟滞——一般 **2 根**确认，恢复 `range` 要 **3 根**，`high_vol_chop` 冲击**立即**生效（风险态宁可误报不可漏报）。v3.1 起还有一条事件门槛：**财报窗内（未来 10 个 ET 日历日）`squeeze → 趋势` 需要 3 根**。

`raw_state`（机器原判）与 `state`（迟滞确认态）双列存储，**当前库内 12.6% 的行两者不同**。

### ② 定位置 —— 4h 关键位区间

**关键位是价格区间，不是价格线。** 六类来源：

| 来源 | 说明 |
|---|---|
| 枢轴簇 | 价格在同一价区反转过的点，**聚类后**成带 |
| `EMA21/55/100/200` | policy 原文指定的周期（注意不是判定层用的 EMA50） |
| 区间上下沿 | 近 60 根的 P90/P10 |
| POC | 币安 1h 量流的成交密集区 |
| 前日高低 / 前周高低 | 公共参照点 |

**宽度怎么来**（这一条经过一次根本性重构）：

- **枢轴簇**：宽度 = **簇内反转价位的散布**。三次触底在 63000/63120/63080 → 散布 0.2 ATR，这是又硬又窄的支撑带；散布 4.6 ATR 的三个点根本不是"一个位"，不合并。
- **单次枢轴 / EMA / 其他单点**：固定 ±0.25 ATR。
- 所有输出区间**互不重叠**（有断言锁定）。重构前最宽的"关键区"达 6.95 ATR，现在最大 1.487 ATR。

> **为什么不用 K 线振幅定宽度**：一根振幅 9 个 ATR 的大阴线，只说明那一刻波动大，**不说明这个价位重要**。用它定宽度等于说"K 线跌得越狠，支撑位越宽"——毫无道理。

**位置语义配合 regime**（这是本层的灵魂）：**同一个价格，在不同状态下是不是关键位、是支撑还是压力、可不可交易，答案不同**。

| regime | `at_support` | `at_resistance` |
|---|---|---|
| `trend_up` | **回踩买点**（需来向 = 从上方回落） | 减仓位，**不是开空位** |
| `trend_down` | 前低处不可交易；EMA100/200 深跌位需二次确认 | **反弹空点**（S1，policy 引用最密的剧本） |
| `range` | **区间下沿**（S3） | **区间上沿**（S3） |
| `squeeze` | 等突破，不可交易 | 同左 |
| `high_vol_chop` | 降频，不可交易 | 同左 |

`middle_zone` 在**所有**状态下一律不可交易。

**两个容易忽略的机制**：
- **角色反转**：一个位被有效击穿后，支撑变压力（policy §2.2 明写）。系统按"建立时角色 vs 当前价格方位"判定，并标注 `role_flipped`。
- **来向（approach）**："**回踩至** EMA21" 是路径语义，不是距离。从上方跌回来是回踩（机会），从下方涨上去是遇阻（完全不同的事）。来向不符时不可交易，理由记 `wrong_approach`。

### ③ 找共振 —— 4h 主导 + 1d/1h 分级辅助

**主导票（必要条件）**：`at_support` 需 4h cRSI **超卖区**、`at_resistance` 需 **超买区**。4h 带内 → 不产生机会。

> **为什么主导票是 4h**：关键位是 4h 定的，"这个位有没有起作用"就该看 4h。1h 超卖可能只是日内噪声。

**辅助票（加减分）**：1d 与 1h 的 cRSI，同向 `+1`、反向 `−1`、带内 `0`、**缺失不计票**（不用 0 伪装中性）。

`score` ∈ [−2, +2] → `grade`：`≥1` **强** / `=0` **中** / `≤−1` **弱**。

> **为什么辅助票不是硬要求**：实测 4h 极值只有 15/71 个品种，其中有辅助票同向的只剩 2 个。硬要求同向会把系统卡死。

**1d 的两个角色**：投辅助票；以及当 `1d regime` 与 `4h regime` 矛盾且至少一方是趋势态时，输出**逆势警示**——"4h 趋势上行 vs 1d 趋势下行：这是逆大势的反弹单"。**警示不否决**：4h 是决策周期，1d 不能推翻它；但你该知道自己在逆势（policy 的 S13 正是用仓位 1/3–1/2 来区分这两类交易的）。

### ④ 风控与警告 —— policy 没有、我们独有的一层

policy 是纯价格结构框架，全文没有波动率与事件概念。这一层补上：

**GATE 1 止损宽度校验**：
```
结构止损 = 命中区的下沿之下（多）/ 上沿之上（空），外推 0.25 ATR
预期波动% = IV3 × √(实际期限 / 365)
ratio = 止损距离% / 预期波动%
ratio < 1.0 → 严重：止损在 1 倍预期波动内，正常波动就会打掉
ratio < 1.5 → 偏紧
```
**这是波动率栈最硬的用途**——ATR 是回顾的，IV 是市场对未来的定价。一个止损在结构上合理、但在波动率上必然被扫，是最常见的亏损方式，纯价格系统看不见它。

**环境与事件标注**（只标注，不改动作）：
- `IV30 分位 > 0.85` → 对应 policy §6.5 极端环境，建议降频降仓
- `3d > 30d 倒挂` → 近端定价高于制度层，短期有事
- **持仓窗内有财报** → 非技术面风险，纯价格系统的盲区

---

## 4. 数据基础

### 品种宇宙

**74 个币安永续合约**：加密 7 / 美股 61 / 商品 5 / 国际 1，分核心池 50 与观察池 24。三个周期 1d / 4h / 1h。

X2 扩容（38→74）的名额分配依据是实测的**张成盲区**——相关性检验显示系统把科技板块看得几乎完美（R²=0.938），却对公用事业（0.044）、能源（0.061）结构性失明，所以名额优先给低 R² 板块，而不是加厚已经看得清的维度。

### 采集节奏

`collector.py` 每 **300 秒**一轮，一轮 13 类 `sync_*`。**四级失败隔离**：治理动作各自 try → K 线按 `(品种,周期)` → 衍生品按品种 → 最底层单接口失败只置空该字段。实测扛过全源断网：库零增长、进程存活、恢复后自愈。

**RTH 门控**：美股相关管线（个股 IV、期限曲线、宽度）只在美股交易时段采，用仓库自己的 NYSE 日历（含 2025–2027 假日表与 6 个半日市）。

### 波动率四层

| 层 | 来源 | 能否算分位 |
|---|---|---|
| **3d 持仓前端** | 币安期权近端 IV（加密+金属）/ moomoo 期限曲线 iv3（美股） | ❌ 无厂商历史，自 08-05 起自攒 |
| **9d 中段** | iv9 / VIX9D | ❌ 个股侧自攒 |
| **30d 制度层** | DVOL（加密）/ moomoo iv30（美股，3.1 年史） | ✅ **唯一能算分位的层** |
| **事件轴** | 财报日历 + `known_at` | 用于条件分位与 v3.1 门槛 |

**3d 层是专为 1–3 天持仓周期建的**——IV30 对这个周期"太深"，它计价的是未来一个月。

**条件分位**：个股 IV 的分位按"未来 30 天内有无财报"条件化。实证依据——事件前 10 日只占样本 15.4%，却占了 IV 分位 ≥0.8 的 **35.2%**：三分之一的"高波信号"其实是日历伪信号。

---

## 5. 两个界面

### 总览页 —— 今天该看哪几个

分层，**机会优先**：

| 分区 | 含义 |
|---|---|
| **armed** | 位置 + 信号双共振，带强度分级 |
| **位置候选（wait_signal）** | 位置到了，信号没到 |
| **接近关键位** | 在关键位附近但当前状态下不可交易 |
| **风险提示** | IV 极值 / 倒挂 / 财报临近 |
| **观望**（默认折叠） | `middle_zone`——大多数品种大多数时候在这 |
| **数据不可用**（默认折叠） | 如实列出，不隐藏 |

机会区为空时会明说"**这是常态，不是故障**"。**刻意不做**的：等待计时器、红点提醒、声音、推送——那些会制造"该做点什么"的焦虑，正是 policy §6.3 踏空否决要防的。

### 详情页 —— 依据全摊开

政策卡把结论拆成可验证的证据：**全部关键位区间表**（距现价 ATR / 区间 / 来源 / 触碰次数 / 当前角色 / 是否 eligible 及不合格原因）、命中行高亮、角色翻转标注、来向路径说明、波动率提示、止损校验。

价格图上：**支撑绿虚线、压力红虚线**，命中的那条加粗。**只在 4h 周期绘制**——关键位是 4h 算的，切到 1d/1h 就不画，避免误以为是当前周期的位。

### VVVhermes

双模：总览页问横截面，详情页问单品种。注入内容包含当前时刻锚点、历史消息时间前缀、以及超过 15 分钟的**时间跳变横幅**（"历史读数只在其时点有效"）。

有一段不可覆盖的 **policy guard**：WAIT 是正常输出、只出建议不下单、消息不构成开仓依据、满仓一律否决改分批。

---

## 6. 术语表

| 词 | 意思 | 别混淆 |
|---|---|---|
| `regime` / 状态 | 五态之一 | 与 policy 的五态不是同一组，本系统直接用自己的 |
| `raw_state` | 机器逐根原判 | `state` 是迟滞确认后的 |
| `candidate` / 酝酿中 | 正在换挡但确认根数未够 | 与"位置到了信号没到"无关 |
| `margin` | 到最近可翻转边界的距离 | **按 raw_state 算**，与确认态可能不同 |
| `zone` / 关键位区间 | 一段价格带 | 不是一条线 |
| `eligible` | 该 zone 在当前 regime 下算不算关键位 | 与"到位了没有"是两件事 |
| `tradeable` | 位置层认可的候选机会位 | **不代表信号与风控已过** |
| `armed` | 位置 + 信号双共振 | 仍未过仓位与盈亏比门槛 |
| `approach` / 来向 | 价格从哪个方向来到这个 zone | 路径语义，不是距离 |
| `grade` 强/中/弱 | 辅助票净分 | 不影响是否 armed，只影响排序与警示 |
| 预览（未收线） | 用形成中的 K 线试算 | **永不入库**，只作预警 |
| ° 标记 | 盘中延迟报价、尚未结算确权 | |

---

## 7. 阈值总表

**所有阈值都是版本化先验，尚未经回测校准。**

| 层 | 阈值 | 当前值 | 来源 |
|---|---|---|---|
| 判定 | er_rank / direction / tilt | 0.60 / 0.30 / 0.10 | v1 先验 |
| 判定 | SQUEEZE_BBW / SQUEEZE_ATR / HIGHVOL_ATR | 0.15 / 0.30 / 0.85 | v1 先验 |
| 判定 | 确认根数 | 2 / range 3 / 冲击 1 / 事件窗 +1 | v1 + v3.1 |
| 关键位 | ZONE_HALF_ATR / MERGE_ATR / MAX_ZONE_ATR | 0.25 / 0.50 / 1.50 | 起步值 |
| 关键位 | PIVOT_CLUSTER_ATR | 0.35 | 起步值 |
| 位置 | AT_ZONE_ATR / APPROACH_BARS | 0.50 / 3 | 起步值 |
| 共振 | 强/弱分界 | +1 / −1 | 用户裁决 |
| 止损 | ZONE_BUFFER_ATR / 三档 ratio | 0.25 / 1.0 / 1.5 | 起步值 |
| IV | IV_RANK_WIN / MIN | 252 / 120 | 现有口径 |

界面上会显示 `先验 lv1/loc1/stop1/vol1 · 未校准`——**这个标记在校准完成前不会消失**。

---

## 8. 运维

| 事项 | 说明 |
|---|---|
| **进程** | `collector` 与 `dashboard` 均**无开机自启**，重启后需手工拉起 |
| **休眠** | Mac 休眠 = 采集暂停。建议插电时禁止自动睡眠 |
| **断网** | 进程不崩，逐轮报错、恢复后自愈。K 线自动补齐；**快照型数据（近端 IV、期限曲线）断多久缺多久** |
| **OpenD** | 登出或版本过旧 → 美股 IV 静默停。心跳条的 OpenD 灯会变色 |
| **日志** | `data/collector.log`（**无轮转**，持续增长） |
| **数据库** | `data/market.db`（WAL），**不在 git 里**，无自动备份 |
| **测试** | `.venv/bin/python -m pytest tests/ -q` |

### 版本纪律

**核心立论**：不同版本产生的状态行混在一起会悄悄污染统计。

| 版本线 | 管什么 | 升版后果 |
|---|---|---|
| `RULES_VERSION` | 判定规则与阈值 | 全量重算确认态 |
| `AUDIT_VERSION` | 审计快照字段集 | 全量重算特征 |
| `LEVELS/LOCATION/STOPCHECK/VOLNOTE` | policy 测量层 | 随 payload 下发，不重算历史 |

**任何阈值改动必须递增对应版本号。** 现在用的是版本谓词：版本不匹配的行视为缺失、下一轮原地重算——不再有全表删除。

---

## 9. 设计纪律

这些是贯穿全系统的硬规矩，读代码时会反复遇到：

1. **算不出就返回 None**——绝不用 0 / 0.5 / 中性值伪装缺失。缺 1d 数据时辅助票记 `None` 而不是 0 票，就是这条。
2. **未收线数据不进判定**——预览单独存、显式标注、永不入库。
3. **source 进主键**——不同数据源的同一指标物理隔离，跨源混算分位在结构上不可能发生。
4. **判定层与决策层分离**——policy 层是纯消费者，单向依赖，绝不修改判定层。
5. **标签只说规则检验过的东西**——见 `high_vol_chop` 的例子。
6. **系统不下单**——输出止于建议与理由。
7. **先诊断后实施**——阈值改动要有证据、要版本化、要用户批准。

---

## 10. 边界与已知限制

**诚实清单**：

- **阈值全部未经回测校准**。它们是先验值，界面上的"未校准"标记不是谦辞。
- **policy 本身的胜率/盈亏比从未被独立验证**（policy 原文 §9 自述）。本阶段的目标是建立明确的分析流程，不是统计验证。
- **3d/9d 层没有历史**，分位要等自攒（美股约 60 个交易日、加密快 5 倍）。
- **不知道你的持仓**。因此 policy 的 §4 建仓政策、§5 持仓管理、以及 GATE 2（风险定仓）、GATE 3（盈亏比）都**没有运行**。系统只能告诉你"位置和信号到了、止损该放哪、够不够宽"，不能告诉你"该下多大仓"。
- **1d 覆盖不全**：X2 扩容的新永续多数 1d 不足 90 根，这些品种没有 1d 底座与辅助票。
- **入场触发层（5m/15m）未建**——从 armed 到实际下单之间的精确择时，目前靠人。
- **无自动备份、无开机自启、无主动告警**（只有面板上的心跳条）。

**最后一句**：这个系统最容易被误用的方式，是把"位置候选"当成"该买了"。位置到了只是四道门里的第一道——信号、止损宽度、盈亏比、仓位，后面每一道都可能把它拦下来。系统会把每一道门的结论都告诉你，**它们全过了才叫机会**。
