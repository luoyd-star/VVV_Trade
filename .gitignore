.venv/
__pycache__/
*.pyc
.DS_Store

# 本地数据库与日志：数百 MB 级且持续增长，靠 collector 自采积累，不入版本控制
data/

# 模型后端配置：可能含 API key，用 agent.example.json 作模板
agent.json

# 本机工具授权，与项目本身无关
.claude/settings.local.json
web/*.bak
