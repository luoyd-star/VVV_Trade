# 路线3：location.py 深审（regime-aware 判定的正确性）
1. **regime × 位置语义表**逐格验证（用户裁决 2 的落地，是本层核心）：
   trend_up/trend_down/range/squeeze/high_vol_chop × at_support/at_resistance，
   每一格的 meaning/tradeable/required_approach 是否与 policy 原文一致？
2. **角色反转**（§2.2「原支撑变压力」）：origin_role vs 当前 role 的判定是否正确？
   role_flipped 在 _eligible 里的用法对不对？举真实例子验证。
3. **approach（来向）**：infer_approach 的三态判定（from_above/from_below/None）
   逻辑是否正确？K=3 根够不够？横盘/区间内起点是否被正确排除？
   已知修复：approach 曾被设计成全局参数导致 trend_up 全部误判，现改为逐 zone 现算——
   验证这个修复是否彻底。
4. middle_zone 在**所有** regime 下 tradeable=False 是否真的成立（P11）？
5. AT_ZONE_ATR=0.5 这个"到位"阈值：用真实数据统计它对分层结果的敏感性
   （0.3/0.5/0.8 各会怎样）。
