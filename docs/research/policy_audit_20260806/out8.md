# 路线8：端到端数值对拍

## 发现

| # | 严重度(P0/P1/P2) | 类型 | 断言（一句话） | 证据 | 建议修法 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | P0 | Policy 严重失真 | 当前实现只有剧本名，没有 `action`、持仓状态、风险定仓、RR、DCA/情绪否决等决策闭环，并且门槛失败不会把剧本降级为 WAIT。 | 原文要求 `position/account/position_building/emotional_flags`：[eric-policy.md:21](/Users/luoyingdong/Documents/VVV_Trade/eric-policy.md:21)，且任一 GATE 失败必须 WAIT：[eric-policy.md:106](/Users/luoyingdong/Documents/VVV_Trade/eric-policy.md:106)。实际 payload 无这些输入和 `action`：[dashboard.py:1142](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1142)；剧本只覆盖五个字符串分支：[dashboard.py:1067](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1067)，先生成 `play`、后算 stop 且不反哺决策：[dashboard.py:1221](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1221)。实跑 JPM：`signal_ok=True, play='S3 区间上沿做空'`，同时 `stop_check.verdict='too_tight', ratio=0.19017`，没有降级。命令：`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -c 'import dashboard; from regime import storage; c=storage.connect_ro(); t=dashboard._tf_payload(c,"JPM-USDT","4h"); p=dashboard._build_policy_payload(c,"JPM-USDT",{"4h":t}); print(p["play"],p["stop_check"])'`。 | 新增单一决策器，输出 `action/reasons/gates`；缺 position/account 或任一 gate 未过时明确输出 `WAIT`，剧本仅作候选，不得作为顶层结论；再接入持仓管理、RR、DCA 和 veto。 | 高 |
| 2 | P0 | 判定矛盾 | `signal_ok` 和 `play` 不检查 `location.tradeable`，会在政策明确不可交易时显示绿色“位置+信号共振”，或在来向失败时仍给出加仓剧本。 | `_signal_ok` 只看 `at` 与 cRSI：[dashboard.py:1056](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1056)；`_policy_play` 不接收 `tradeable/reason`：[dashboard.py:1067](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1067)；前端 `signal_ok=true` 即绿色共振：[app.js:435](/Users/luoyingdong/Documents/VVV_Trade/web/app.js:435)。当前库实跑：XPD 为 `high_vol_chop, meaning=high_vol_reduce_frequency, tradeable=False`，却得 `signal_ok=True`；DIS 为 `tradeable=False, reason=wrong_approach`，却得 `S5 突破回踩加仓（位置到了信号没到）`。复现命令：`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -c 'import dashboard; from regime import storage; c=storage.connect_ro(); [(lambda p,l: print(s,l["tradeable"],l["reason"],p["signal_ok"],p["play"]))(dashboard._build_policy_payload(c,s,{"4h":dashboard._tf_payload(c,s,"4h")}),dashboard._build_policy_payload(c,s,{"4h":dashboard._tf_payload(c,s,"4h")})["location"]) for s in ("DIS-USDT","XPD-USDT")]'`。 | 将 `oscillator_match` 与 `policy_confluence` 分开；只有 `tradeable=True`、路径门槛满足且矩阵允许开仓时才能标“共振”，否则结论必须是 WAIT/降频及对应理由。 | 高 |
| 3 | P0 | 波动率口径 | 实际 1.67 天、单到期近邻 IV 被丢掉期限和质量元数据后标成“IV3”，并按 3 天计算预期波动，可能直接改变止损宽度结论。 | `_xopt_block` 明确返回 `tenor_days/method/n_expiries`，并声明这些字段必须出到消费方：[dashboard.py:249](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:249)；装配层却只保留 `near["iv"]`：[dashboard.py:1288](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1288)；随后固定用 3 天公式和 “IV3” 文案：[volnote.py:37](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/volnote.py:37)、[stopcheck.py:97](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/stopcheck.py:97)。SQL：`sqlite3 -header -column "file:data/market.db?mode=ro" "SELECT iv,tenor_days,method,n_expiries FROM opt_iv_near WHERE symbol='DOGE-USDT' ORDER BY ts DESC LIMIT 1;"` 输出 `46.48, 1.67, nearest, 1`；实算代码输出 `policy_3d=4.2139%, native_tenor=3.1440%, overstatement=34.0%`。详情 IV 卡同时显示 `~1.7d·单点`：[app.js:792](/Users/luoyingdong/Documents/VVV_Trade/web/app.js:792)，政策卡却显示 `3日预期波动 ±4.2%（IV3 46.5）`。 | 贯穿传递实际期限、方法、到期数和采集时间；只有可靠插值得到严格 3d IV 时才称 IV3；单到期近邻若要外推必须显式标“代理”，超过期限容差时按铁律返回 `None`，不得进入 stop verdict。 | 高 |
| 4 | P0 | 阈值治理 | location、volnote、stopcheck 的先验阈值没有版本号，levels 的 `lv1` 又未进入 dashboard/Hermes 出口，违反“阈值必须版本化并标注来源”的铁律。 | 只有 `[levels.py:18](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:18)` 定义 `LEVELS_VERSION`；location 阈值见 [location.py:14](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/location.py:14)，波动阈值见 [volnote.py:12](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/volnote.py:12)，止损阈值见 [stopcheck.py:12](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/stopcheck.py:12)，均只有“起步值、待校准”注释。`_build_policy_payload` 的返回键不含任何 policy/threshold 版本：[dashboard.py:1142](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1142)。实跑 `grep -nE 'VERSION|起步值' regime/policy/*.py` 仅命中 `LEVELS_VERSION="lv1"` 及各未版本化常量。 | 建立统一 `POLICY_THRESHOLDS_VERSION` 和机器可读 manifest，逐项记录值、来源、适用资产/周期、校准状态；将版本及 manifest 摘要注入详情、overview 与 Hermes。 | 高 |
| 5 | P0 | 时点一致性 | OHLCV 与 regime 分两次提交，而 policy 不核对两者最新 `ts`，新收线或状态计算失败期间会把新价格配旧 regime 生成错误建议。 | OHLCV 在 [storage.py:319](/Users/luoyingdong/Documents/VVV_Trade/regime/storage.py:319) 写入后立即 `commit()`；状态到 [collector.py:619](/Users/luoyingdong/Documents/VVV_Trade/collector.py:619) 才计算，并在 [collector.py:654](/Users/luoyingdong/Documents/VVV_Trade/collector.py:654) 提交。扫描端分别读取 df 和 `_latest_state`，没有比较时间戳：[dashboard.py:1323](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1323)。当前库核对暂为零错位：`WITH o AS (...MAX(ts)...ohlcv...), r AS (...MAX(ts)...regime_history...) SELECT ... WHERE o.ots<>r.rts;` 输出 0 行，但两次提交间窗口由代码确定存在。 | `_latest_state` 返回 `state+ts+version`；仅当 state.ts 与决策 OHLCV 最新 ts 完全相等且版本符合时继续，否则返回 `None`/`regime_pending`；更佳方案是同一事务发布可消费 snapshot。 | 高 |
| 6 | P0 | 跨出口一致性 | overview 固定缓存 60 秒而详情页每次实时重算，数据更新边界上主页与详情页可同时展示不同 zone、cRSI、IV 或 play。 | 详情入口每次调用 `build_dashboard`：[dashboard.py:1009](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1009)；overview 缓存定义及直接返回见 [dashboard.py:1470](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1470)。复现现有测试：`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/pytest -q -s -p no:cacheprovider tests/test_dashboard_overview.py::test_overview_payload_uses_60_second_cache` 输出 `1 passed`；测试明确断言 t+59 秒仍返回同一对象：[test_dashboard_overview.py:111](/Users/luoyingdong/Documents/VVV_Trade/tests/test_dashboard_overview.py:111)。 | 两个出口消费同一个带 `snapshot_id/as_of_bar_ts` 的不可变 policy snapshot；详情从 snapshot 取政策块，或要求客户端携带 overview 的 snapshot id；不能只靠各自重算。 | 高 |
| 7 | P1 | 出口可审计性 | 路线8的五个可计算样本后端数字一致，但主页和 Hermes 大量删字段/删品种，导致同一 zone 无法从卡片继续追到 `<panel>`。 | 首页机会卡只显示标签、距离、touches，不显示 lo/hi：[overview.js:94](/Users/luoyingdong/Documents/VVV_Trade/web/overview.js:94)；near payload 直接丢掉 zone、touches、play：[dashboard.py:1406](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1406)；Hermes 只逐条列 opportunity 与 risk：[agent.py:410](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:410)，middle 仅数量：[agent.py:443](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:443)。当前六样本 `<panel>` 实跑：JPM 有完整机会行；DIS、DOGE 仅风险行；CL、XPD、GS 为 `<ABSENT>`。后端固定快照对拍值：JPM `[356.97,360.799357] / touches=4 / dist=0 / cRSI=63.85`；DIS `[98.02,99.88] / 14 / 0.065789 / 72.96`；CL `[74.7425,76.2465] / 6 / 0 / 31.35`；DOGE `[0.0687423214,0.06977] / 10 / 0 / 43.69`；XPD `[1347.600143,1370.302857] / 7 / 0 / 78.17`；GS 正确返回 4h 缺失。上述五个样本的 overview 原始 item 与详情 hit-zone 的 lo/hi/touches 均相等，cRSI 也与详情现有 4h cRSI 卡相等。 | overview 至少保留命中 zone 的 lo/hi/touches/play/degraded；Hermes 列出 near 与 unavailable 的逐品种事实，middle 可继续压缩，但应允许按 symbol 查询完整快照。 | 高 |
| 8 | P2 | 测试覆盖 | 现有 65 个 policy 测试全部通过，但没有真实数据库六态跨出口 golden test，因此上述门槛、期限、缓存与字段抑制问题均未被测试捕获。 | 实跑：`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/pytest -q -s -p no:cacheprovider tests/test_policy_levels.py tests/test_policy_location.py tests/test_policy_volnote.py tests/test_policy_stopcheck.py tests/test_policy_batch3.py tests/test_dashboard_overview.py` 输出 `65 passed in 0.34s`。当前测试主要分别断言模块或合成 payload，例如 [test_policy_batch3.py:21](/Users/luoyingdong/Documents/VVV_Trade/tests/test_policy_batch3.py:21) 与 [test_dashboard_overview.py:87](/Users/luoyingdong/Documents/VVV_Trade/tests/test_dashboard_overview.py:87)。 | 增加只读固定快照 fixture，覆盖五态+缺数据，逐项断言 DB bar/state ts、zone、dist、cRSI、IV 期限元数据、action/gates、overview、详情和 Hermes；另加更新边界与错误状态的竞争测试。 | 高 |

## 自查盲区

- 数值快照对应最新 4h 开盘时间戳 `1785931200000`；库持续更新后应重新执行所列 SQL/命令。
- 未调用真实 Hermes 模型或写入 chat；只核对了实际 `<panel>` 注入文本，因此模型最终措辞不在本次只读审查证据内。
- 未启动或重启服务，也未做浏览器截图；主页/详情文案依据实际 payload 与前端渲染代码逐字段核对。
- 按用户裁决未做回测；只执行单元测试、只读 SQL 和确定性现算。
- 当前库有 74 个 symbol、72 个具备 4h、35 个具备 1d；GS 作为数据不足样本，1d 缺失品种均保留 4h 判断。