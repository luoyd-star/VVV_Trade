# 路线9：失败模式与降级路径
1. 逐个构造异常输入，验证不崩且不产生错误判断：
   ATR=0 / 价格为 NaN / zones 为空 / regime 是未知值 / cRSI 全 NaN /
   OHLCV 只有 90 根（刚过闸）/ vol1h 完全缺失 / IV 全缺
2. **degraded 传播**：哪些降级会静默？哪些会显式标注？有没有"算不出却给了结论"的路径？
3. unavailable 列表：reason 是否覆盖所有排除原因？有没有品种被静默丢弃？
4. 新鲜度：overview 用的 4h K 线可能是几小时前的——有没有陈旧数据被当成"当前位置"？
   已知详情页有 1.5×TF 的 stale 闸，overview 有没有对应机制？
5. 时区：所有时间戳的时区处理（UTC 存储、ET 交易日、JST 显示）在新代码里是否一致？
