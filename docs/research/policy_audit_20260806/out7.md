# 路线7：Hermes 集成（单品种 + 横截面双模）

## 发现

| # | 严重度(P0/P1/P2) | 类型 | 断言（一句话） | 证据 | 建议修法 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | P0 | 单品种注入缺失 | 单品种 Hermes 的 `<panel>` 完全丢弃已装配的 policy 块，连“剧本成立但结构止损严重偏紧”也看不到，会造成决策层严重失真。 | [dashboard.py:1018](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1018) 构建 policy，[dashboard.py:1040](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1040) 放入 payload；但 [regime/agent.py:149](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:149) 至 [regime/agent.py:386](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:386) 未读取 `p["policy"]`。实跑 `build_dashboard("JPM-USDT")`：`PAYLOAD_PLAY S3 区间上沿做空`、`SIGNAL_OK True`、`STOP verdict=too_tight, ratio=0.1902`；`render_context` 对 `policy`、剧本、`stop_check`、`位置+信号` 的包含检查全部为 `False`。 | 给 `render_context()` 增加专用 policy 渲染段，至少注入：4h/1d、命中区间及来源、角色/来向、`signal_ok`、剧本、波动提示、止损宽度、降级原因，并明确“建议而非交易指令”；overview 与 symbol scope 二选一，不会重复。 | 高 |
| 2 | P1 | 横截面字段缺失 | overview 页面宣称 Hermes 能读“接近”横截面，但实际只注入接近数量，完全省略 near 明细、unavailable 原因、五路心跳和 payload 时间。 | payload 在 [dashboard.py:1454](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1454) 返回上述全部分区及 [dashboard.py:1500](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1500) 的 heartbeat；[regime/agent.py:410](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:410) 至 [regime/agent.py:444](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:444) 只展开 opportunity、risk 和 middle 数量；[web/overview.html:126](/Users/luoyingdong/Documents/VVV_Trade/web/overview.html:126) 却声称可读“机会、接近、风险与观望”。当前实跑：`near=28, unavailable=2, heartbeat=5`，注入文本只有“接近 28 个”，没有这 28 个 symbol。 | 注入 near 的轻量字段（symbol、4h、位置语义、距离、cRSI），同时注入 unavailable 的 symbol/reason、heartbeat 状态和 `updated_at`；若主动省略某分区，应在 UI 和 legend 明示“只提供数量”。 | 高 |
| 3 | P1 | 统计口径说明 | `risk` 是可与四个主分区重叠的标签，但 Hermes 收到的是“分层统计”且 legend 未说明重叠，容易把 74 个品种误读成 131 个分类样本。 | [dashboard.py:1429](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1429) 先独立加入 risk，随后 [dashboard.py:1438](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1438) 再进入 opportunity/near/middle，证明 risk 非互斥；[regime/agent.py:403](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:403) 称“分层统计”，[regime/agent.py:84](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:84) 未解释重叠。只读现算：`15+28+29+2=74`，加入 `risk=57` 后显示总数为 131。SQL `SELECT COUNT(DISTINCT symbol) FROM ohlcv;` 输出 `74`。 | 把文案改成“主分区（互斥）”与“风险标签（可重叠）”两组，并在 `PANEL_LEGEND` 明确 risk count 不能与其他 counts 相加。 | 高 |
| 4 | P1 | PANEL_LEGEND | overview 注入了 `at_resistance`、`range_edge_opportunity`、`from_above/None`、`1d=None` 等原始枚举，但 legend 没解释其含义及 1d 缺失时仍保留 4h 判断的降级口径。 | 注入点见 [regime/agent.py:423](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:423)；overview legend 只有 [regime/agent.py:84](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:84) 至 86 三行概述，没有 `at/meaning/approach/None` 字典。当前实跑文本大量出现 `位置=at_resistance(range_edge_opportunity,...来向None)` 和 `1d=None`。 | 在 legend 增加枚举字典，并明确 `1d=None=底座缺失、4h 判断降级保留`；更稳妥的是渲染时直接输出中文含义，避免模型自行猜枚举。 | 高 |
| 5 | P1 | 建议措辞防线 | 送模提示词没有“系统不下单、建议不得升级为买卖指令”的硬约束，当前自定义提示还要求“就行动、不要机械地再等等”，与 policy 的 WAIT-first 原则正面冲突。 | [hermes_system.md:6](/Users/luoyingdong/Documents/VVV_Trade/hermes_system.md:6) 至 [hermes_system.md:17](/Users/luoyingdong/Documents/VVV_Trade/hermes_system.md:17) 要求在不确定下行动并避免“再等等”；[eric-policy.md:7](/Users/luoyingdong/Documents/VVV_Trade/eric-policy.md:7) 明确大多数时刻正确动作是 WAIT；网页免责声明位于 [web/overview.html:68](/Users/luoyingdong/Documents/VVV_Trade/web/overview.html:68)，但 [regime/agent.py:665](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:665) 的送模 system 没有注入该免责声明。 | 在不可被自定义人设覆盖的机制层追加硬规则：只输出“建议关注/等待/风险条件”，不得使用“应该买入、立即做空”等执行措辞；所有 OPEN 剧本必须同时陈述未校验的 GATE，并默认降级为 WAIT。 | 高 |
| 6 | P2 | 上下文预算 | 当前 overview 上下文尚未明显撑爆配置模型，但历史仅按“20 条×每条 8000 字符”裁剪，缺少 provider-aware token 上限，最坏输入可达约 17.3 万字符。 | [regime/agent.py:674](/Users/luoyingdong/Documents/VVV_Trade/regime/agent.py:674) 至 680：每条截 8000 字符、保留 20 条。现算：overview context `7003` 字符，完整基础 system `11793` 字符；库内最近 20 条历史 `26444` 字符，当前总输入约 3.8–4.0 万字符，粗估约 1.9–4.0 万 token。理论上限约 `11793+20×8000=171793` 字符，粗估约 8.6–17.2 万 token。风险列表单独占 `3759/7003` 字符。 | 按实际模型 tokenizer 和上下文窗倒序装配历史；为 system/panel/history/output 分配预算；优先压缩重复 risk 文案，同时保留被遗漏的 near 核心字段。 | 中 |

## 自查盲区

- scope 与时间感知未发现代码缺陷：定向测试命令输出 `3 passed in 0.31s`；手工 overview 组装得到 `CURRENT_ANCHOR=True`、`GAP_NOTICE=True`、`LEGEND_TIME=True`，历史消息同时带 `[时间·距今]` 和 `[scope:overview/symbol]`。
- 当前库尚无 `chat.scope` 列：`sqlite3 "file:data/market.db?mode=ro" "PRAGMA table_info(chat);"` 只返回 `id/ts/role/content`。代码会在首次聊天写入时惰性迁移，内存库测试已通过；本次受只读要求约束，未在生产库触发该迁移。
- `127.0.0.1:8787` 当前未运行，无法做浏览器→HTTP→数据库的完整链路验证；`curl` 返回连接失败，未重启服务。
- 未调用真实 Codex provider，避免消耗订阅并产生临时输出文件；第 5 条是确定的提示词约束缺口，不是对某次模型回复的经验抽样。
- 环境没有可用 tokenizer，token 数采用中英混合文本约 1–2 字符/token 的区间估算。