# 路线5：overview 横截面的正确性与性能

## 发现

| # | 严重度(P0/P1/P2) | 类型 | 断言（一句话） | 证据 | 建议修法 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | P0 | 时点一致性 | overview 既不校验 4h K线与 regime 的时间戳一致，也不持有全扫描只读事务，采集提交期间可能用新价格配旧 regime，并让 74 品种来自不同数据库快照。 | `dashboard.py:1051-1053` 丢弃 regime 时间戳；`dashboard.py:1324-1329` 先读 K线再独立读状态；`dashboard.py:1480-1494` 未执行 `BEGIN`。写端 `regime/storage.py:326` 先提交 OHLCV，之后 `collector.py:619-654` 才计算并提交状态。实跑：`c.execute("SELECT COUNT(*) FROM ohlcv").fetchone(); print(c.in_transaction)` 输出 `False`，证明各 SELECT 不共享事务快照。当前只读 SQL 静态检查为 `ts_mismatch=0/72`，但不能消除上述每轮采集必然存在的中间窗口。 | `_latest_state` 返回 `ts+state`，强制 `state_ts == df末根ts`，不一致时进入 `unavailable: regime_4h_lagging`；全量扫描开始时执行只读 `BEGIN`，完成后关闭事务。最好再将单品种 OHLCV 与状态写入合并为一个事务。 | 高 |
| 2 | P0 | Policy 保真 | `signal_ok` 只看支撑/压力和当前 cRSI 三值，无法识别 regime 是否存在开仓单元格，也无法满足 S13 的“第二次超卖”，因此会错误宣告 P06 共振。 | `eric-policy.md:77` 要求第二次超卖/底背离，`eric-policy.md:115` 要求矩阵位置与信号同时满足；实现 `dashboard.py:1056-1064` 只接收 `at, crsi_zone`。实跑输出：`trend_down at_support 超卖区 signal_ok=True play=S13…（需二次确认）`；同时 `high_vol_chop`、`squeeze` 的支撑超卖均得到 `signal_ok=True, play=None`。现库详情实跑：`UBER-USDT/XPD-USDT/XRP-USDT` 均为 `tradeable=False, signal_ok=True, play=None`；前端 `web/app.js:435-440` 却显示“位置 + 信号共振”。 | 将原始读数拆成 `extreme_aligned` 与真正的 `p06_ok`；后者必须接收 regime、location meaning 和剧本。S13 用 cRSI 历史识别“退出超卖后再次进入”的第二个 episode；历史不足返回 `None`。没有 OPEN 单元格的 regime 也返回 `None`，不能返回 `True`。 | 高 |
| 3 | P1 | 风险完备性 | `risk` 只能与成功完成位置扫描的品种重叠，无法与 `unavailable` 重叠，现库因此漏掉 GS、SMH 已经可计算的高 IV 风险。 | `dashboard.py:1324-1331` 在缺 4h/regime 时提前返回，风险计算到 `dashboard.py:1365-1369` 才发生；`dashboard.py:1422-1427` 对 unavailable 直接 `continue`。但 `regime/policy/volnote.py:44-48` 的高 IV 分位提示不依赖价格。现库实跑：`GS-USDT {'iv30_rank':0.905} → ['隐含波动历史高位…']`，`SMH-USDT {'iv30_rank':0.869} → 同类提示`；overview 却输出 `risk=57`，两者只在 unavailable。 | 将风险装配独立于位置扫描，在结构提前返回前仍计算可用的风险项；scan 顶层携带 `_vol_notes`，partition 即使 `item=None` 也允许同时加入 risk。 | 高 |
| 4 | P1 | 1d 降级传播 | 1d 缺失在详情页安全保留，但 overview 的 near/middle/risk 会删除该字段，Hermes 对 opportunity 又输出字面量 `1d=None`，未做到全链路显式降级。 | SQL：`SELECT tf,COUNT(DISTINCT symbol)…` 输出 `1d=35, 4h=72`，即 39/74 缺 1d。`dashboard.py:1376` 初始 item 保留 `regime_1d`，但 `_overview_lite` 的 `dashboard.py:1406-1415` 删除它，risk 的 `dashboard.py:1431-1436` 也未携带。现库实跑：缺 1d 分布为 opportunity 9、near 16、middle 12、unavailable 2；near/middle 示例键中 `regime_1d=<omitted>`；`regime/agent.py:423-427` 输出 `1d=None`。详情实跑 APP 则正确为 `regime_1d=None, degraded=['regime_1d_missing', …]`。 | lite/risk 均保留 `regime_1d` 和结构化 `degraded`；前端显示“1d 不可用，保留 4h 判断”；Hermes 将 `None` 翻译成“不可用”，并注入缺失 1d 的数量，不能只写“1d 底座”。 | 高 |
| 5 | P1 | 并发缓存 | 60 秒缓存没有 single-flight，且前端在慢请求完成前每秒重复触发 `load()`，一次冷刷新可并发执行多轮 74 品种扫描。 | `dashboard.py:1470-1503` 是无锁全局字典；服务使用 `ThreadingHTTPServer`（`dashboard.py:1765`）。`web/overview.js:362-368` 在倒计时为 0 时每秒调用 `load()`，而 `nextRefresh` 直到请求结束才更新（`web/overview.js:212-230`）。四线程冷缓存复现输出：`threads 4, scan_calls 8, expected_single_fill 2, distinct_payload_objects 4`。现库冷扫描均值 1.383 秒，已超过前端 1 秒重触发周期。 | 后端增加锁/Condition 的 single-flight，并在锁内二次检查缓存；TTL 用 `time.monotonic()`。前端增加 `loading` 标志，或在发起请求时立即推进 `nextRefresh`，禁止在途重入。 | 高 |
| 6 | P1 | 时间语义 | Hermes 忽略 payload 的 `updated_at`，把缓存期内的旧横截面标成提问时刻的“此刻快照”。 | payload 在 `dashboard.py:1497` 写入扫描开始时间，缓存期内原样返回；但 `regime/agent.py:391-401` 重新取当前墙钟并断言“下方全部数据为此刻横截面快照”。复现传入 `updated_at=1000`，输出仍为 `当前时刻: 2026-08-05 16:26 UTC——下方全部数据为此刻横截面快照`。缓存期内数据库更新不会使 payload 失效，最大可错标约 60 秒。 | Hermes 同时输出“提问时刻”和“横截面生成时刻”，后者必须取 `ov.updated_at`，并标注快照年龄；超过约定阈值时明确写“缓存快照”。 | 高 |
| 7 | P2 | 性能/N+1 | 一次 overview 冷扫描执行 719 条 SELECT，存在明显按品种 N+1 和重复财报/IV查询，虽然当前单机耗时尚可。 | `dashboard.py:1483-1485` 串行扫描；每品种又读 OHLCV、两周期状态、vol1h 和风险源（`dashboard.py:1324-1367`）。trace 实跑：总计 `719 SELECT`，分布为 `ohlcv 75, regime_history 144, vol1h 72, earnings 236, stock_iv_term 60, stock_vol 59, stock_vol_live 60, opt_iv_near 10, dvol 2, heartbeat 3`。5 次应用缓存冷扫描：1.353–1.427 秒，均值 1.383 秒；每轮调用 `get_ohlcv=74`、其余计算=72，cRSI 约 0.65 秒、levels 约 0.44–0.47 秒、vol inputs 约 0.17 秒；缓存命中约 `0.0000007s`。`dashboard.py:383-385` 与 `1282-1285` 还重复查询 earnings proximity。 | 批量预取最新 4h/1d 状态、期限结构、IV、earnings 和近端期权数据；复用已有横截面查询思路；为 overview 提供仅算末值、不遍历背离的轻量 cRSI 路径。先解决 single-flight，再做 CPU 优化。 | 高 |

## 自查盲区

- 现库快照核验通过：主分层 `15+28+29+2=74`，唯一品种 74、重复 0；risk 当前 57 条均与主分层重叠。风险对 unavailable 的漏标见发现 3。
- 排序核验通过：opportunity 中 `dist_atr=0` 的 9 个品种按 symbol 排序，完整排序键为 `(dist_atr, symbol)`，刷新不会因平局乱跳。
- `opportunity` 按 `regime/policy/location.py:3-5` 的明示语义是“位置候选”，不是 P06 已完成；现库 15 个候选中仅 2 个 `signal_ok=True`、13 个为等待信号，未将这一设计本身列为缺陷。
- 定向测试命令 `PYTHONPATH=/Users/luoyingdong/Documents/VVV_Trade PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -s -p no:cacheprovider tests/test_dashboard_overview.py tests/test_policy_batch3.py tests/test_policy_location.py` 输出 `49 passed in 0.25s`。
- 遵守“不重启进程”，未对正在运行的 HTTP 服务做真实并发抓包；并发结论来自同一代码路径的四线程复现。性能数字是本机、操作系统页缓存已热但 overview 应用缓存清空的结果。