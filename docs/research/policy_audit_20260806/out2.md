# 路线2：levels.py 深审（关键位提取的正确性）

## 发现

| # | 严重度(P0/P1/P2) | 类型 | 断言（一句话） | 证据 | 建议修法 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | P0 | 聚类/区间上限 | 1.5 ATR 上限只限制单个分片，拒并后产生的重叠分片仍组成最大 6.917 ATR 的连续关键区，链式效应并未真正挡住。 | [`levels.py:175-188`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:175)：超限成员直接新建下一簇，没有消除与上一簇的区间重叠。只读全宇宙实跑：`PIVOT_PRECAP max=8.6728, >1.5=279/576(48.44%)`；截断后 `SINGLE 1359、MERGED 737，>1.5=0`；但输出有 `437` 对相邻重叠，形成 `132` 个重叠连通块，其中 `123` 个跨度仍大于 1.5 ATR，最大 `DOGE-USDT=6.917 ATR/10个分片`，42 个连通块包含当前价。 | 增加“输出区间不得重叠”的最终不变量；超过上限的重叠连通块应标记 `chain_overflow/contested` 并禁止直接进入位置判断，或按确定性切点分成互不重叠的区间，而不是留下相互覆盖的分片。 | 高 |
| 2 | P0 | 支撑/压力语义 | 聚类会把支撑和压力合成一个 zone，再用多数票或“低价成员”任意决定角色，当前已有 17 个价格正处于这种矛盾 zone 内。 | [`levels.py:124-131`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:124)执行角色投票；[`location.py:111-113`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/location.py:111)在价格位于区间内时直接沿用该投票结果。实跑 737 个输出簇：`92` 个混合支撑/压力，覆盖 `60/72` 品种，`17` 个含当前价；例如 BTC 当前 zone 同时含 `poc(support)`、`prev_day_hi/pivot_high(resistance)`，SOL 同时含两类角色。 | 不得为冲突成员投票；保留 `member_roles`，将结果标成 `contested`，或在聚类阶段禁止异角色合并，再由 regime-aware 的 `location` 层裁决。 | 高 |
| 3 | P0 | 前日/前周日界 | 前日/前周固定按 UTC 00:00 切桶，与库内 BTC/ETH/SOL 的 Deribit 1d 08:00 UTC 日界不一致，已经输出错误高低点。 | [`levels.py:195-208`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:195)固定 `floor("D")` 和 UTC 周一。SQL：`SELECT source,tf,strftime('%H:%M',ts/1000,'unixepoch'),COUNT(DISTINCT symbol) FROM ohlcv WHERE tf='1d' GROUP BY 1,2,3;` → `binance_futures|1d|00:00|32`、`deribit|1d|08:00|3`。当前 BTC 前日：代码按 `2026-08-04 00:00~05 00:00` 得 `64489/63281.5`，1d 原生 `2026-08-04 08:00` 为 `64489/63413.5`；前周代码高点 `65726`，1d 口径为 `65672.5`。ETH 前日低点相差 `5.4`，SOL 相差 `0.052`。同一 SQL 对32个 Binance 源品种比较为 `32|0 mismatched`，问题明确集中在3个 Deribit 主价源品种。 | 调用方传入源的 session anchor，或直接传入同源 1d 序列计算前日/周；周界也应以该日界锚定，不能只移动日线而仍按 UTC 周一零点。 | 高 |
| 4 | P0 | NaN/ATR 铁律 | ATR 在验证 OHLC 有限性之前计算，Pandas 横向 `max` 会跳过 NaN，导致缺失高价数据时仍伪造 ATR 和关键位而不是返回 None。 | [`levels.py:291-300`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:291)先算 ATR，直到 [`levels.py:309-315`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:309)才拒绝坏 OHLC；[`volatility.py:13-20`](/Users/luoyingdong/Documents/VVV_Trade/regime/features/volatility.py:13)的横向 `max` 默认 `skipna=True`。复现：240根 `high=NaN, low=99, close=100` → `atr=1.0, zones=1`，而非 `atr=None/zones=None`。当前库 SQL 检查 `52334` 根4h数据暂为 `null_ohlc=0, invalid_ohlc=0`，但边界行为违反铁律。 | ATR 前验证所需窗口内 OHLC 全部有限且满足 `high≥max(open,close,low)`、`low≤min(open,close,high)`；否则直接 `no_atr`。底层 `true_range` 也应使用不跳过 NaN 的归约。 | 高 |
| 5 | P1 | touches 因果性 | touches 是先用当前完整历史确定区间、再回扫整段历史，因而把枢轴形成及确认之前对同一价格的穿越计成“触碰”。 | [`levels.py:97-108`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:97)扫描整个 df；枢轴 zone 在 [`levels.py:316-330`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:316)建成后立即回扫，而枢轴实际需 [`structure.py:13-25`](/Users/luoyingdong/Documents/VVV_Trade/regime/features/structure.py:13)右侧4根确认。576个当前枢轴实跑：实现统计 `6539` 次；按“形成事件一次＋确认后独立重测”仅 `1293` 次，`545/576` 个枢轴被高估。连续停留按一次计算本身正确。该数字会直接展示在 [`overview.js:94`](/Users/luoyingdong/Documents/VVV_Trade/web/overview.js:94)。 | 每个来源携带 `known_at_ts`；枢轴仅计形成事件和确认后的重新进入。EMA 应按历史动态 EMA 带计算 respect，而不是用今天的固定 EMA 区间回扫；暂时无法定义的来源应输出 `touches=None`。 | 高 |
| 6 | P1 | POC 还原误差 | 把整小时成交量全部压到该小时 VWAP 桶不能还原真实 volume-profile POC，现有字段甚至无法识别真实误差。 | [`levels.py:247-255`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:247)只使用 `quote_vol/volume`，再把全部小时量放入一个桶。对69个 OHLC 与量流均为 Binance 的同源品种、最近16560小时实跑：小时高低范围中位数 `0.309 ATR`，`60.39%` 大于0.25 ATR桶宽，P99 `1.937 ATR`，最大 `6.729 ATR`。构造与每小时 `volume/quote_vol/high/low` 完全一致的“成交量仅分布在小时高低端点”数据，所得 POC 相对当前代理变化：P50 `0.5 ATR`、P90 `2.05`、P95 `3.15`、最大 `4.75 ATR`，`62/69` 品种改变；这证明误差不可由当前聚合字段约束，并非实际误差估计。 | 若要称为 POC，应采集 Binance 1m 或逐笔/聚合成交构建价格量分布；现实现应改名 `hourly_vwap_volume_proxy`、显式标注代理误差，并禁止它单独扩大可交易 zone，只作为共振标记。 | 高 |
| 7 | P1 | POC 覆盖守门 | POC 覆盖率以“自身最后一小时”为窗口终点而非4h决策时点，因此连续旧数据达到80%时，量流即使已滞后约72小时仍会通过。 | [`levels.py:240-245`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:240)令 `end=frame最后时间`；dashboard 只多取260小时（[`dashboard.py:49-50`](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:49)）。复现192个连续小时：`coverage=0.8, result=True, reason=None, poc=100.5`。按260小时查询余量计算，最近约72小时完全缺失时仍可保留192个旧小时并通过。当前库72个可计算品种均为 `240/240、lag=0`，所以本次快照未触发。 | `_poc_level` 增加 `expected_end_ts`，固定检查决策时点之前240个预期小时；末小时滞后或时间槽覆盖不足时返回 `poc_stale/poc_coverage_insufficient`。 | 高 |
| 8 | P1 | EMA 预热/调用窗口 | dashboard 只加载400根4h数据计算 EMA200，`adjust=False` 的启动残差使当前 EMA200 相比库内完整同源历史最多偏移0.301 ATR。 | [`dashboard.py:49`](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:49)限制400根；[`dashboard.py:1162`](/Users/luoyingdong/Documents/VVV_Trade/dashboard.py:1162)据此调用；EMA实现见 [`structure.py:40-41`](/Users/luoyingdong/Documents/VVV_Trade/regime/features/structure.py:40)。64个具有≥200根数据的品种实跑：48个实际被截断；400根值对完整历史值差异 P50 `0.0367 ATR`、P90 `0.1250`、最大 `0.3012`；10个大于0.1 ATR、2个大于0.25 ATR。BNB为 `581.6143 vs 579.9418`。 | EMA使用完整可用历史或足够的专用 pre-roll 后再裁剪枢轴/区间窗口；输出应记录 EMA 输入根数或 convergence 状态。 | 高 |
| 9 | P1 | 周期完整性 | 前日/前周只要目标桶内存在一根数据就会输出极值，部分周期会被伪装成完整周期。 | [`levels.py:209-216`](/Users/luoyingdong/Documents/VVV_Trade/regime/policy/levels.py:209)仅判断 highs/lows 非空。复现14根4h数据从周日开始，目标前周实际仅有周日6根：输出 `PREV_WEEK_EMITTED=True, mid=106.0`，且 `prev_week_history_insufficient=False`。 | 按输入周期和 session anchor 校验完整槽位；4h 的完整前日应有6根、完整前周42根且无缺口，不能满足时对应来源返回 None。 | 高 |

## 自查盲区

- 库内没有1m或逐笔成交，因此不能计算“真实 POC 误差”；第6项给出的是不可识别性证明及与现有聚合字段完全一致的敏感性情景，不是实际误差估计。
- 最新 snapshot 有74个品种，但 `GS-USDT`、`SMH-USDT` 没有4h数据；全宇宙数值统计实际覆盖72个，二者只能验证为诚实降级。
- 只读权限下 pytest 因无法创建临时文件而未能启动；改为直接调用 `tests/test_policy_levels.py` 的6个测试函数，输出 `DIRECT_TESTS 6 PASS`。现有测试没有覆盖跨角色聚类、输出区间重叠、源日界、NaN ATR、旧 POC 或部分前周。
- 已核且未发现独立问题：枢轴右侧 `k=4` 确认无未来函数；ATR≤0会返回 `no_atr`；连续驻留只计一次；单个原始/合并 zone 的1.5 ATR上限本身有效；32个 Binance 00:00 日界品种的前日、前周4h聚合与1d完全一致。