# 路线5：overview 横截面的正确性与性能
1. overview_payload 的分层逻辑：opportunity/near/risk/middle/unavailable 五类
   互斥性与完备性——有没有品种被漏掉或重复计数？risk 与其他类可重复是否被正确处理？
2. **排序**：机会区按 dist_atr 升序——但 dist_atr=0 的品种很多（都在区间内），
   平局怎么排？排序是否稳定（刷新时不乱跳）？
3. 60 秒缓存：缓存键是什么？并发请求会不会各算一遍？缓存期内数据更新了怎么办？
4. 性能：74 品种 × (get_ohlcv 400 根 + extract_levels + locate + crsi) 的实际耗时，
   实测并给出数字。有没有 N+1 查询？
5. **1d 底座缺失降级**：已修复为保留 4h 判断。验证降级后 regime_1d=None 在
   全链路（payload/前端/Hermes）的传播是否安全，有没有地方把 None 当成某个状态。
6. signal_ok 的判定：at_support 要 cRSI 超卖区、at_resistance 要超买区——
   与 policy P06「位置+信号共振」是否一致？cRSI 的 zone 三值（超买区/超卖区/带内）
   够不够用？
