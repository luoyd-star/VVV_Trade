# 路线11：与既有系统的一致性（这一层是新客，别把老规矩打破了）
1. **依赖方向**：policy 层是否真的单向依赖（policy → classify/features/storage 只读）？
   有没有反向依赖或循环 import？classify.py 是否真的一行未改？
2. **命名与口径**：新代码用的术语（zone/level/at_support/play/tradeable）与既有体系
   （state/regime/candidate/margin）是否冲突或含义重叠？有没有同名不同义？
3. **数据纪律**：新代码有没有违反既有的"source 进主键""未收线不入判定"
   "分位只在结算序列上算""跨源不混算"？
4. 新代码是否落库？如果落了，表结构是否遵循既有纪律（UTC 毫秒/幂等 upsert/版本列）？
   如果没落库（每次现算），point-in-time 可复现性怎么保证？
5. 测试：新增测试的质量——是真钉住不变量，还是宽松断言侥幸绿着？
   有没有测试覆盖不到的关键路径？
6. 文档：这一层的设计文档（docs/DESIGN_POLICY_LAYER_20260805.md）与实际实现是否一致？
   已有偏离要指出来。
