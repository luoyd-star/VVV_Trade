# 路线5：regime/ 全模块注释与 docstring 对实现
逐文件过 regime/*.py 与 regime/features/*.py 的每一段 docstring 与行内注释：
1. 描述的行为与代码实际行为是否一致（分支条件、默认值、返回语义、异常路径）；
2. 注释里的"实测数字"用今天的库现算复核（已知 structure.py 50.8% 与 classify.py 两条不必重复）；
3. 引用的 file:line 或函数名是否还存在（重构后注释常指向已消失的东西）；
4. 中文术语是否与面板/文档一致（如"结算/实时/预览/影子/代理"）。
collector.py 与 dashboard.py 的注释也在范围内（这两个文件最大且改动最频）。
