# 路线6：Python 死代码

## 发现

| # | 类型(不一致/死代码/干扰) | 断言（一句话） | 证据A (file:line + 内容) | 证据B (file:line / grep / SQL) | 建议处置 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | 死代码 | `fetch_overview()` 全仓零调用，生产已改走同一 moomoo 接口的 `fetch_live()`。 | `regime/moomoo_iv.py:71-94` 定义 `fetch_overview`；`collector.py:258` 调用 `moomoo_iv.fetch_live(...)`。 | `grep -RInw --include='*.py' fetch_overview .` → 仅定义行；`grep -RInw --include='*.py' fetch_live .` → 定义及 `collector.py:258`。 | 删除旧函数；若需保留历史接口，至少标记 deprecated 并补调用测试。 | 高 |
| 2 | 死代码 | 单品种 `binance_opt_iv.snapshot()` 零调用，生产只使用批量版 `snapshot_all()`。 | `regime/binance_opt_iv.py:153-163` 定义单品种包装；`collector.py:173` 调用 `snapshot_all(now_ms=now)`。 | `grep -RInw --include='*.py' snapshot .` → 唯一相关可执行命中为该定义；`grep -RInw --include='*.py' snapshot_all .` → 定义及 `collector.py:173`。 | 删除单品种包装，或为人工探测建立明确 CLI。 | 高 |
| 3 | 死代码 | `ref_daily` 有持续写入且已存 15,331 行，但唯一读取函数 `get_ref_daily()` 零调用，因此目前没有程序化消费者。 | `regime/storage.py:465-479` 写入；`regime/storage.py:482-487` 唯一 SELECT 包装；`collector.py:529,535` 持续写入。 | `grep -RInw --include='*.py' get_ref_daily .` → 仅 `storage.py:482`；SQL：`SELECT count(*) FROM ref_daily;` → `15331`。全仓 `ref_daily` 命中除建表/写入/该死 getter 外仅注释和 Hermes 查询提示。 | 明确接入研究/耦合消费者；否则暂停维护性写入并把表标记为 dormant。 | 高 |
| 4 | 死代码 | `get_breadth()` 是零调用的预留读取 API；当前宽度管线只有采集写入。 | `regime/storage.py:706-720` 为写端；`regime/storage.py:723-729` 定义读端；`collector.py:120,143` 明写“只采不消费”并调用写端。 | `grep -RInw --include='*.py' get_breadth .` → 仅定义行；SQL：`SELECT count(*) FROM breadth;` → `2`。 | 若两年后才评审，暂删 getter、启用时再按真实消费口径实现；或明确标注 future API。 | 高 |
| 5 | 死代码 | `YEAR_DAYS` 是零引用常量，期限计算实际直接使用毫秒到天的除数。 | `regime/binance_opt_iv.py:37`：`YEAR_DAYS = 365.0`；`regime/binance_opt_iv.py:100` 直接除以 `86_400_000.0`。 | `grep -RInw --include='*.py' YEAR_DAYS .` → 仅 `regime/binance_opt_iv.py:37`。 | 删除。 | 高 |
| 6 | 死代码 | 五个 import 在各自模块中没有任何加载：`dashboard.datetime`、`backfill_earnings.date`、`exp_event_gate.np`、`coupling_fsm.field`、`run_coupling_m2b.FSMParams`。 | `dashboard.py:16`；`scripts/backfill_earnings.py:17`；`scripts/exp_event_gate.py:17`；`regime/coupling_fsm.py:15`；`scripts/run_coupling_m2b.py:24`。 | 对各文件运行 `grep -nE`：`np`、`field`、`FSMParams` 均只命中 import；`date` 只有 import，其他是 `.date()` 方法；顶层 `datetime` 未使用，`dashboard.py:429` 另行局部导入为 `_dt`。AST Load 复核均为 0。 | 删除五个 import。 | 高 |
| 7 | 死代码 | `_live_iv_block()` 的 `settled_last` 和 `in30` 两个形参从未在函数体读取。 | `dashboard.py:397` 定义两参数；`dashboard.py:397-468` 函数体改为自行计算 `in30_now`，没有读取二者；`dashboard.py:380` 仍传入 `last, in30`。 | `sed -n '397,468p' dashboard.py` 后 grep 两参数 → 仅函数签名一行。 | 从签名和调用点同时删除这两个参数。 | 高 |
| 8 | 死代码 | `collector.sync_moomoo_history()` 内部函数 `incr()` 的 `tag` 参数无任何用途，但调用方分别传入 `"iv"`、`"optstat"`。 | `collector.py:300-315`：签名含 `tag`，函数体不读；`collector.py:321-328` 传入两个字符串。 | 在 `collector.py:300-315` 内 grep `tag` → 仅签名。 | 删除 `tag` 形参及两个实参。 | 高 |
| 9 | 死代码 | `rolling_sign_rate()` 的 `window_frac` 默认参数从未被读取，也从未有调用方传非默认值。 | `regime/coupling.py:209-227`：函数体只使用 `halflife`；`regime/coupling.py:235` 唯一调用为 `rolling_sign_rate(z)`。 | `grep -n window_frac regime/coupling.py` → 仅签名；`grep -n 'rolling_sign_rate(' regime/coupling.py` → 定义和一个默认调用。 | 删除参数；若原本想控制窗口，先实现其语义再保留。 | 高 |
| 10 | 死代码 | `e1_estimator_race()` 的 `tf` 参数始终被传入，但函数完全不读取它。 | `regime/experiments.py:54-92`：`tf` 只在签名；`regime/experiments.py:269` 调用 `e1_estimator_race(df, tf)`。 | 在 `54-92` 行内 grep `tf` → 仅签名。 | 删除形参与实参。 | 高 |
| 11 | 死代码 | `run_experiment(..., note="")` 的 `note` 参数不影响 payload 或实验 ID，CLI 传参属于冗余；备注实际由 `record_experiment()` 单独处理。 | `regime/backtest.py:347-418`：`note` 只在签名；`scripts/run_backtest_p0.py:150` 传入，`:156` 又独立传给 `record_experiment`；`regime/backtest.py:459` 才真正写入 ledger。 | 在 `backtest.py:347-418` 内 grep `note` → 仅签名。 | 从 `run_experiment` 删除 `note`，保留 `record_experiment(..., note=...)`。 | 高 |
| 12 | 死代码 | `pair_table()` 中 `n_rows = len(r)` 是零读取的死局部变量。 | `regime/coupling.py:230-250`；`:236` 赋值，覆盖率从 `:237-247` 使用的是 `valid_rows` 和 `n_joint`。 | `grep -nw n_rows regime/coupling.py` → 仅 `:236`；全仓其他 `n_rows` 命中均属于不同作用域/字典键。 | 删除赋值。 | 高 |
| 13 | 死代码 | `run_coupling_grid.py` 留有三个互不生效的残余：常量 `REP_PAIRS_IDX`、列表 `reps` 和计时变量 `tc`。 | `scripts/run_coupling_grid.py:38`、`:106`、`:111`；实际功效计算在 `:118-119` 直接使用 `strong`、`edge`，进度耗时用总计时 `t0`。 | `grep -nw REP_PAIRS_IDX scripts/run_coupling_grid.py`、`grep -nw reps ...`、`grep -nw tc ...` → 各仅赋值一处。 | 删除三处。 | 高 |
| 14 | 死代码 | `run_coupling_m2b.power_test()` 先计算的小时/位置混合 `delay` 在读取前立即被第二套统一算法覆盖。 | `scripts/run_coupling_m2b.py:72-74` 第一次赋值；`:75-78` 注释“统一按 bar 数”并重新赋值；首次值未被读取。 | `grep -n 'delay =' scripts/run_coupling_m2b.py` → `:72`、`:77` 两次赋值；首次赋值与覆盖之间无读取。 | 删除 `:72-74`，保留统一 bar 算法。 | 高 |
| 15 | 干扰 | CLI 文件头仍把系统称为“v1”，但当前唯一规则版本常量是 v3.1，容易让读者误判 CLI 使用旧规则。 | `main.py:2`：`市场状态系统 v1`；`main.py:14,64` 实际直接调用现行 `regime.classify`。 | `regime/classify.py:44`：`RULES_VERSION = "v3.1"`；命令 `grep -nE '市场状态系统 v1\|RULES_VERSION' main.py regime/classify.py` 得两侧。 | 改成“当前规则版本由 `RULES_VERSION` 决定”，避免再硬编码版本。 | 高 |
| 16 | 不一致 | `run_coupling_m2.py` 用法说明宣称写入 `prior-1`，实际导入并写入的是 `calibrated-1`。 | `scripts/run_coupling_m2.py:7-8`：`threshold_version=prior-1`；`:22,49,75` 使用导入的 `THRESHOLD_VERSION`。 | `regime/coupling_fsm.py:22`：`THRESHOLD_VERSION = "calibrated-1"`。 | 更新 docstring，并说明该脚本会按“当前阈值代”重放，而非复现 prior-1。 | 高 |
| 17 | 干扰 | 已死的 `fetch_overview()` docstring 仍称 3303 数据等于昨结且“须在 RTH 复测”，同文件现行实现已经确认该接口盘中滚动。 | `regime/moomoo_iv.py:71-80`：旧函数使用 `get_option_underlying_overview`；`:74-75` 写“上一交易日结算值、须复测”。 | `regime/moomoo_iv.py:97-113`：现行 `fetch_live` 使用同一 API；`:100-102` 明写“盘中滚动更新”。 | 随死函数一并删除；若保留历史说明，改成带日期的已推翻结论。 | 高 |
| 18 | 死代码 | 参考实现中的 `calibrate_t_amp()` 和 `continuous_series()` 仓内零调用，连该文件的 `__main__` 自检/示例也未覆盖。 | `regime-spectrum/04-REFERENCE-IMPL.py:174-192`、`:196-214` 定义；`:294-305` main 只调用 `_selftest`、`describe_path`、`spectrum_summary`、`membership`。 | 分别执行 `grep -RInw --include='*.py' calibrate_t_amp .`、`grep -RInw --include='*.py' continuous_series .` → 均只有定义行。未发现针对这些名字的 `getattr`、动态 import、`eval/exec`。 | 若是参考 API，加入 main 示例或自检以证明可运行；否则从“可运行参考实现”中移除。 | 中 |

## 自查盲区

- 未把 `main.py`、`vvvhermes.py` 或 `scripts/` 下任何整个文件判死：这些文件多数是人工入口，且最近提交均在 2026-08-02 至 08-05；仅凭“无自动调用方”不足以证明无人手工使用。
- 静态搜索已检查别名导入和常见动态调用，但无法证明仓库外的个人脚本没有直接导入公开函数；因此参考实现两项仅给中置信。
- 未运行运行时 coverage；不可达性检查覆盖了常量条件、终止语句后代码和明显覆盖赋值，复杂数据相关分支仍需动态覆盖才能下结论。