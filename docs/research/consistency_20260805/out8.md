# 路线8：数据库死物与写读不对称
## 发现
| # | 类型(不一致/死代码/干扰) | 断言（一句话） | 证据A (file:line + 内容) | 证据B (file:line / grep / SQL) | 建议处置 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | 不一致 | `storage.py` 文件头的“collector 写、dashboard 读”已不是实际拓扑：19 张业务表包含脚本写入、Hermes 读写、实验读取及四张硬编码零读表。 | `regime/storage.py:1`：`collector 写、dashboard 读`。 | 完整账本：`ohlcv` W=`collector.py:577`/`scripts/backfill_history.py:146`，R=`collector.py:592`/`dashboard.py:78`/`regime/coupling.py:142`/`regime/backtest.py:300`；`dvol` W=`collector.py:765`，R=`dashboard.py:277`；`regime_history` W=`collector.py:599,631`，R=`dashboard.py:109,854`/`regime/agent.py:379`/`regime/backtest.py:308`；`deriv` W=`collector.py:665,689,694,721`，R=`dashboard.py:646,758-760`/`regime/backtest.py:338`；`usvol` W=`collector.py:402,422`，R=`dashboard.py:529,617`；`chat` W=`dashboard.py:1164-1165`/`vvvhermes.py:55-56`，R=`dashboard.py:1100,1159`/`vvvhermes.py:47`；`live_bars` W=`collector.py:580`，R=`dashboard.py:176`；`vol1h` W=`collector.py:498,510`，R=`dashboard.py:141`；`stock_vol` W=`collector.py:322`，R=`dashboard.py:336`/`regime/agent.py:421`；`stock_iv_term` W=`collector.py:220`，R=`dashboard.py:667,689`（SQL 当前 0 行，代码路径活）；`opt_iv_near` W=`collector.py:178`，R=`dashboard.py:237,300`；`stock_vol_live` W=`collector.py:260`，R=`dashboard.py:405`；`earnings` W=`collector.py:339`/`scripts/backfill_earnings.py:53`，R=`collector.py:617`/`dashboard.py:425,498`；`meta` 双向；`stock_option_stat` 仅运维水位读；`ref_daily/breadth/bbo/universe_snapshot` 无硬编码业务读；`sqlite_sequence` 为 `chat` 自增内部表。SQL：`SELECT COUNT(*) ... name<>'sqlite_sequence'`＝19。 | 改文件头为真实角色模型；最好生成一份机器可检验的表级 W/R 清单。 | 高 |
| 2 | 死代码 | `storage.py` 仅有两个存取函数零调用：`get_ref_daily()` 和 `get_breadth()`。 | `regime/storage.py:482-487` 定义 `get_ref_daily`；`regime/storage.py:723-729` 定义 `get_breadth`。 | grep：`grep -RInE '\b(get_ref_daily|get_breadth)\b' --include='*.py' --include='*.sh' --exclude='storage.py' --exclude-dir=.venv --exclude-dir=.git --exclude='*.bak' .`；输出 0 行。 | 若近期无消费计划则删除函数；若保留影子数据，应增加明确消费者/验收脚本。 | 高 |
| 3 | 死代码 | `ref_daily` 已存 15,331 行且每日续写，但全仓零硬编码消费者；“耦合历史层”目前实际仍从 `ohlcv` 取数。 | `regime/storage.py:465-479` 称其为“耦合研究历史层”并写表；`collector.py:529-540` 每日写 BTC/Yahoo；`scripts/backfill_reference.py:63-77` 还执行全量回填。 | 唯一 SELECT 在死函数 `regime/storage.py:482-487`；上述零调用 grep 输出 0。实际耦合读取 `regime/coupling.py:141-143` 的 `storage.get_ohlcv(...)`。SQL：`SELECT COUNT(*),COUNT(close),COUNT(source) FROM ref_daily;`＝`15331,15331,15331`。 | 明确选择：接入耦合/实验，或暂停日更并标成归档研究料；否则持续制造无消费数据。 | 高 |
| 4 | 死代码 | 已知的 `bbo`、`universe_snapshot` 可精确到所有列均无读者：包括主键、价格/数量以及全部宇宙属性列。 | schema：`regime/storage.py:56-69`；写入：`collector.py:454-455`、`collector.py:475-478`。 | grep：`grep -RInE '(SELECT|FROM|JOIN).*?(bbo|universe_snapshot)' ...` 输出 0 行。SQL：`bbo` 22,517 行，`bid/bid_qty/ask/ask_qty` 各 22,517 非空；`universe_snapshot` 224 行，`pool/theme/class/valid_from` 各 224 非空。故 `bbo.{symbol,ts,bid,bid_qty,ask,ask_qty}` 与 `universe_snapshot.{snapped_at,symbol,pool,theme,class,valid_from}` 全部零消费。 | 在报价质量评审、PIT 宇宙回测接入前暂停写入或加保留期；不能只保留“未来可能用”。 | 高 |
| 5 | 死代码 | `breadth` 的 11 个指标列全部已落库但零消费，且对应读取函数本身也是死代码。 | `regime/storage.py:104-110` 定义 `denom/up_1d/up_20d/up_60d/up_250d/up_ytd/tail_up/tail_dn/flat/total/captured_at`；`collector.py:143` 写入。 | SQL：2 行且上述 11 列各有 2 个非空值。`get_breadth` 仅定义于 `regime/storage.py:723`；排除定义后 grep 0 行。 | 若按影子字段继续积累，补一份覆盖/质量审计消费者；否则删除死 accessor，并给表设明确转正日期及保留政策。 | 高 |
| 6 | 死代码 | `stock_option_stat` 的八个指标列均未进入 payload、判定或实验；生产读取仅为确定增量起点的 `ts` 水位。 | `regime/storage.py:668-699` 定义并整列读写八指标；`collector.py:326-329` 明示“纯采集，不进判定”。 | 调用 grep 仅见 `collector.py:327`、`scripts/backfill_moomoo_iv.py:49`、测试和定义；通用增量器在 `collector.py:302-311` 只访问 `have["ts"]`。SQL：46,432 行；`option_volume/call_volume/put_volume` 各 46,432，`pc_volume_ratio` 46,416，`option_oi/call_oi/put_oi/pc_oi_ratio` 各 46,352 非空。 | 作为影子表保留可以，但应登记研究入口、转正门槛和保留期；否则八列都是持续增长的采而不用。 | 高 |
| 7 | 死代码 | `regime_history.rules` 236,830 行全部有 JSON，却从未被任何状态 payload、回测、实验或查询助手读取。 | `regime/storage.py:398-407` 每次 upsert 写入并更新 `rules`；生产值来自 `regime/classify.py:317` 的 `json.dumps(r.rules)`。 | SQL：`SELECT COUNT(*),COUNT(rules) FROM regime_history;`＝`236830,236830`。`get_states` 在 `storage.py:424-432` 不选 rules；`get_states_audit` 在 `storage.py:497-512` 也不选。针对 `dashboard.py regime/agent.py regime/backtest.py regime/experiments.py scripts/exp_event_gate.py web/app.js` grep `rules` 输出 0。 | 审计价值若真实存在，应把它接入 `get_states_audit`/`vvvquery states`；否则停止写列并规划迁移。 | 高 |
| 8 | 死代码 | `deriv.oi_notional` 已存 65,224 个非空值，但衍生品面板、Hermes和回测只消费原始 `oi`，名义价值完全闲置。 | 生产者在 `regime/deriv.py:42`、`:83` 计算 `oi_notional`；存储列见 `regime/storage.py:117`。 | SQL：`SELECT COUNT(*),COUNT(oi_notional) FROM deriv;`＝`92215,65224`。面板只请求 `oi/premium/taker_ratio/iv30/funding`：`dashboard.py:757-760`；回测只请求 funding：`regime/backtest.py:338`。针对 payload/实验文件 grep `oi_notional` 输出 0。其余七列中 `kind` 在 `storage.py:591-597` 参与结算行过滤，不能误报为死列。 | 要么用 `oi_notional` 做跨品种可比的 OI 变化/排名，要么停止落库；原始合约张数通常不适合横截面对比。 | 高 |
| 9 | 死代码 | `stock_vol.underlying_price` 47,185 行全非空，但落库后没有任何生产消费。 | `regime/storage.py:631-637` 写入，`:657-665` 读取进 DataFrame。 | SQL：`SELECT COUNT(*),COUNT(underlying_price) FROM stock_vol;`＝`47185,47185`。`dashboard.py:336-393` 只使用 `ts/iv/hv`；针对 dashboard、agent、backtest、experiments、前端 grep `underlying_price` 输出 0。 | 若要做 IV 与标的价格同日对账则接入审计；否则停止持久化该冗余副本。 | 高 |
| 10 | 死代码 | `opt_iv_near.index_price` 56 行全非空，但它只随快照落库，从未进入近端 IV payload 或实验。 | `regime/storage.py:761-766` 写入，`:771-777` 返回该列。 | SQL：`SELECT COUNT(*),COUNT(index_price) FROM opt_iv_near;`＝`56,56`。`dashboard.py:237-249` payload 使用 `iv/tenor_days/method/n_expiries/ts`，明确漏掉 `index_price`；下游 grep 输出 0。 | 若用于 IV/标的同步审计就加入健康检查；否则从存储接口移除。 | 高 |
| 11 | 死代码 | `stock_vol_live` 中 `hv_30d/vendor_iv_rank/vendor_iv_pct/call_volume/put_volume` 五列全部满存，却没有进入任何 payload、判定或实验。 | schema/列族：`regime/storage.py:92-97,780-811`；采集器每轮写入见 `collector.py:258-260`。 | SQL：1,766 行，上述五列各 1,766 非空。`dashboard.py:405-467` 只消费 `ts/iv/pre_iv/pc_volume_ratio`；针对 dashboard、agent、backtest、experiments、前端 grep 五列输出 0。注意 `pc_volume_ratio` 已在 `dashboard.py:463-465` 进入 payload，不列为死列。 | 只保留实时卡实际需要的列，或把厂商 rank/HV 接入对照审计；避免每五分钟复制无用字段。 | 高 |
| 12 | 死代码 | `earnings.pub_type`、`period` 和 `known_at` 均不被生产读取；其中 `known_at` 虽为未来严格 as-of 预留，当前 23 个非空值仍不参与任何过滤。 | 写入及保留语义：`regime/storage.py:825-833`；`earnings_event_windows` 明确“不按 known_at 过滤”：`storage.py:838-847`。 | SQL：602 行，`pub_type=602`、`period=602`、`known_at=23` 非空。生产 SELECT 在 `storage.py:849-850,932-934,966-969` 及 `dashboard.py:424-427,497-500` 均只取 `ts` 或 `symbol,ts`；grep `pub_type/period/known_at` 在生产消费者中输出 0。 | `known_at` 应保留并接入预登记的严格 as-of；`pub_type/period` 若不展示、不分组则停止采集或明确其未来用途。 | 高 |
| 13 | 死代码 | meta 精确核验只有共享简报已知的 15 个硬死键；新增死物是活键 `status` 内的 `symbols` 字段，而 `stock_iv_term_last='0'` 不是死键。 | meta 活跃读写骨架：`collector.py:82-89`；`status.symbols` 每轮写入于 `collector.py:806-811`，读取侧 `dashboard.py:872-885` 只提取 `interval/cycle_sec/errors`。`stock_iv_term_last` 在 `collector.py:197-202` 满足 RTH/OpenD 后交给 `_should`。 | SQL 21 族合计 338：`deriv_backfilled_v2_*`、`funding_hist_at_*`、`funding_interval_*`、`funding_interval_at_*` 各74；死 `deriv_backfilled_*` v1=10；死 `regime_audit_*`=5；`usvol_csv_at/max/ok_*` 各5；另12个单键族。死键 grep：`regime_audit_*` 与 `deriv_backfilled_[A-Z]*` 在活动源码输出0。SQL：`status.symbols` 数组长74；grep `status.get("symbols")`/`status["symbols"]` 输出0。SQL：`stock_iv_term_last='0'`；字符串 `"0"` 为真且 `now-0>3600`，下次合格调用会在 `collector.py:88` 自覆写。 | 删除15个已知死键；停止写 `status.symbols` 或让健康页消费；不要把 `stock_iv_term_last=0` 误删为死键，可改成显式 reset 语义减少误解。 | 高 |
| 14 | 干扰 | 运维计数只统计19张业务表中的4张，面板又只展示其中3张，新升级的数据栈几乎不可见。 | `regime/storage.py:1054-1057` 的 `counts()` 仅枚举 `ohlcv/dvol/regime_history/deriv`。 | `dashboard.py:885` 把该字典放入 payload；`web/app.js:1038-1041` 只显示 `ohlcv/dvol/regime_history`，连已计算的 `deriv` 都不显示。SQL：业务表共19张；15:35:26 JST 行数还包括 `stock_vol=47185`、`stock_option_stat=46432`、`vol1h=201866`、`usvol=25896` 等。 | 把函数改名为 `core_counts`，或动态/显式覆盖全部采集表并显示每管线行数与最新时间。 | 高 |
| 15 | 干扰 | Hermes 的只读 SQL 提示仍只列8张表，遗漏11张现存表，包括最近升级的四层波动率与财报表。 | `regime/agent.py:457-459` 列出：`ohlcv/regime_history/deriv/vol1h/usvol/dvol/stock_vol/ref_daily`。 | SQL schema 有19张业务表；遗漏 `bbo/breadth/chat/earnings/live_bars/meta/opt_iv_near/stock_iv_term/stock_option_stat/stock_vol_live/universe_snapshot`。其中反而列出的 `ref_daily` 是本审查确认的零硬编码消费表。 | 将提示改为“示例表”并提供 `.schema`/表清单查询，或由 `sqlite_master` 自动生成，避免每次升级手工漂移。 | 高 |

## 自查盲区

- “零消费者”按硬编码生产调用定义；`scripts/vvvquery.py:81-96` 允许用户执行任意只读 SELECT，因此所有表理论上都可被临时人工查询。
- 未逐键展开 `regime_history.features` JSON 的内部字段消费；本轮只确认了整列 `rules` 和 `meta.status.symbols`。
- 数据库有运行中的 collector 并发追加；行数为 **2026-08-05 15:35:26 JST** 快照，引用关系与零消费结论不受后续增量影响。