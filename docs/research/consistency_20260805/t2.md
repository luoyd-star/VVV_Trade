# 路线2：README.md 全文逐节对现实
README 是入口文档，被 SYSTEM_LOG_20260805 点名「四天里只改过 4 次而 regime/ 改了 40 次」。
逐节核对：架构描述、品种数、表清单、特征清单、版本号、路线图勾选状态、
安装/运行说明（命令还能不能跑）、文件结构树、阈值示例。
每个断言当作待证伪命题，用代码/库现查。已知的 a7/23 项与 DATA_DEGRADED 两条不必重复，
但 README 其余段落全部要过。
