# 路线9：术语与命名口径
## 发现

| # | 类型(不一致/死代码/干扰) | 断言（一句话） | 证据A (file:line + 内容) | 证据B (file:line / grep / SQL) | 建议处置 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | 干扰 | 最新系统总览仍把 `high_vol_chop` 称为“高波动无序”，但现行规则明确没有证明“无序”，正式名称已改为“高波动非趋势（趋势条件未齐）”。 | `SYSTEM_LOG_20260805.md:19`：五状态列为“高波动无序”。 | `regime/classify.py:69-79`：注释明确“没有证明无序”，`STATES["high_vol_chop"] = "高波动非趋势（趋势条件未齐）"`。 | 修正最新系统日志的一句话定义；旧历史日志如需保留旧称，应加“当时术语，现已废弃”。 | 高 |
| 2 | 死代码 | 后端输出的 `states_map` payload 键全仓无消费者；前端继续维护独立 `SM`，使状态中文名仍是双源。 | `dashboard.py:991-996`：dashboard payload 写入 `"states_map": STATES`。 | 零引用证明：`grep -RIn --exclude='*.pyc' --exclude='*.bak' --exclude-dir=.git --exclude-dir=.venv --exclude-dir=.pytest_cache 'states_map' collector.py dashboard.py main.py vvvhermes.py regime scripts tests web`，唯一输出为 `dashboard.py:995`。前端实际使用 `web/app.js:5-10` 的硬编码 `SM`。 | 二选一：前端消费 `states_map`，颜色单独维护；或删除死 payload，并明确中文标签由前端独立管理。前者可消除再次漂移。 | 高 |
| 3 | 不一致 | moomoo 的厂商聚合 `iv` 被 dashboard payload 和 Hermes 提升为严格“30天/iv30”，同一名字实际承载了 moomoo 聚合 IV 与 CBOE 常数期限 IV30 两种概念。 | `dashboard.py:573-586`：`_stock_iv_block()` 的 moomoo 值直接返回为键 `iv30`；`regime/agent.py:61-64` 又称“个股IV=30天隐含波动率（moomoo口径）”。 | `regime/moomoo_iv.py:6-8`：源字段是加权法未公开的自家期权链聚合 `iv`，与 CBOE `iv30`、ORATS `iv30d` 不同；`docs/RESEARCH_IV_PLATFORMS_20260804.md:30-31` 更明确写“非严格常数期限 IV30”。 | 将 payload 主线改名为 `stock_iv`/`vendor_iv`；只有 CBOE 字段使用 `iv30`。Hermes 改称“moomoo 标的级聚合 IV（非严格常数30天）”。 | 高 |
| 4 | 不一致 | `opt_iv_near` 会保存不足3天的 nearest IV，但历史 payload 丢弃 `tenor_days` 后统一改名 `iv3`，前端曲线又统一标为“IV3/3d隐含”。 | `regime/binance_opt_iv.py:94-95,147-150`：夹不住3天时取最近到期，并保存实际 `tenor_days`；`dashboard.py:298-314`：历史仅复制 `ts,iv`，键改为 `iv3`，没有逐点期限。 | `web/app.js:715-716,752-757`：曲线固定称“IV3/3d隐含”。只读 SQL：`SELECT symbol,count(*),round(min(tenor_days),2),round(max(tenor_days),2),group_concat(DISTINCT method) FROM opt_iv_near GROUP BY symbol;`；结果 DOGE 为 **2.07–2.21d nearest**，XAU/XAG 为 **2.61–2.75d nearest**，并非3d常数期限。 | 历史 payload 保留逐点 `tenor_days/method`；曲线名称改为“近端IV”，仅 `method=interp AND tenor_days=3` 时标 IV3。 | 高 |
| 5 | 不一致 | 页面 footer 宣称“时间均为 UTC”，但同页采集器日志尾部使用宿主机本地时间且没有时区标识；当前实际为 JST。 | `collector.py:69-77`：日志格式仅 `%(asctime)s`，未设置 UTC converter 或时区后缀；`dashboard.py:875-886`：日志行原样送往前端。 | `web/index.html:143`：footer 写“时间均为 UTC”。复核命令：`tail -n 3 data/collector.log; date; date -u`；日志为 `2026-08-05 15:26:30`，同时宿主为 `15:29 JST / 06:29 UTC`。 | 日志 formatter 固定 UTC 并追加 `Z`，或保留本地时间但追加 `JST`；footer 改成限定语“行情图时间均为 UTC”。 | 中 |
| 6 | 不一致 | `deriv.ts` 在同一表内同时表示墙钟采样时刻、资金费结算时刻、统计点时刻和 K 线开盘时刻，不满足统一的“bar 开盘 ts”或“观测时刻”语义。 | `regime/deriv.py:32-45`：快照 `ts=time.time()`；`regime/deriv.py:69-84`：funding 使用 `fundingTime`，OI 使用接口 `timestamp`。 | `regime/deriv.py:96-104`：premium 使用 K 线 `k[0]`，即区间开盘时刻；`regime/storage.py:32-35`：所有这些值共用 `deriv(symbol,ts)` 主键，只有 funding 另有 `kind`。 | 至少形成字段契约：`observed_at`、`effective_at`、`bar_open_ts` 分开；更稳妥的是按指标拆窄表，避免 `ts` 同名多义。 | 高 |
| 7 | 不一致 | 同为“接口无时间戳时记录本地采样时刻”，`breadth` 使用 `captured_at`，近端 IV 和盘中个股 IV 却使用通用 `ts`，观测时刻列命名没有统一口径。 | `regime/breadth.py:16,75`：明确返回体无时间戳，采样墙钟写 `captured_at`；`regime/storage.py:104-110` 同时保存交易日锚 `ts` 和 `captured_at`。 | `regime/moomoo_iv.py:97-105,111-125`：同样因返回体无时间戳，却把采集墙钟写成 `ts`；`regime/binance_opt_iv.py:60,79` 也把采集时刻写成 `ts`，对应表结构为 `regime/storage.py:87-97`。 | 建立统一命名：数据有效时刻用 `ts/effective_at`，采集墙钟统一用 `captured_at`；迁移前先在 schema 文档列出每表语义。 | 高 |
| 8 | 死代码 | dashboard 为近端 IV 和盘中个股 IV 输出的两个 `captured_at` payload 字段均无前端、Hermes 或测试消费者。 | `dashboard.py:244-249`：`xopt.captured_at`；`dashboard.py:454-462`：盘中 IV `captured_at`。 | 零引用证明：`grep -RIn --exclude='*.pyc' --exclude='*.bak' --exclude-dir=.git --exclude-dir=.venv --exclude-dir=.pytest_cache 'captured_at' web regime/agent.py tests`，输出 **0 行**；`grep -n '\"captured_at\"' dashboard.py` 仅命中上述两个生产点。 | 若要展示精确 as-of，则接入前端/Hermes；否则删除两个死键，保留已实际消费的 `age_min`。 | 高 |

## 自查盲区

- `states_map`、payload `captured_at` 的零消费者结论限于本仓；无法排除仓外客户端直接消费 dashboard API。
- `ref_daily.series` 的 `BTC/GC/GLD/CL` 与内部 `XXX-USDT` 映射缺少集中契约，但未找到相互冲突的现行描述，因此未列为发现。
- 数据库实际 `source` 值域已核对，现有值均为小写规范拼写；未发现足够证据支持新的大小写或拼写冲突。