# 路线14：端到端对拍——同一个数字的所有出口

## 发现

| # | 类型(不一致/死代码/干扰) | 断言（一句话） | 证据A (file:line + 内容) | 证据B (file:line / grep / SQL) | 建议处置 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | 不一致 | BTC DVOL 当前值出口一致为 34.0，但同一分位 payload/Hermes 为 0.003，前端却四舍五入成 0.00。 | `dashboard.py:277-287` 从 `dvol` 取最后值并用 365 点严格小于分位；`dashboard.py:308-309` 输出 `iv_last`/`iv_rank`。`web/app.js:679-680` 以 1 位/2 位显示，故分位 0.003→0.00；`regime/agent.py:183-184` 直接输出 0.003。 | SQL：`WITH w AS (SELECT ts,dvol FROM dvol WHERE currency='BTC' ORDER BY ts DESC LIMIT 365),cur AS (SELECT * FROM w ORDER BY ts DESC LIMIT 1) SELECT datetime(cur.ts/1000,'unixepoch'),cur.dvol,1.0*SUM(w.dvol<cur.dvol)/COUNT(*) FROM w,cur;` → `2026-08-04 / 34.04 / 0.002740`。`data/collector.log:124820` 记录 `DVOL BTC 最新 34.0`。 | DVOL 分位统一显示 3 位，或统一改成百分数 `0.27%`；不要一个出口显示 0.00、另一个显示 0.003。 | 高 |
| 2 | 不一致 | NVDA 结算 IV 的正式分位实际是“504 日窗内、同财报状态条件分位”，Hermes 固定图例仍声称是普通 252 日分位。 | `dashboard.py:504-514`：取 `s.tail(504)`，剔除当日后只与相同 `in30` 状态比较；`dashboard.py:381-388` payload 为 `rank/rank_raw/rank_kind/earn_in30`。当前链为 `last=47.285 → rank=0.092、rank_raw=0.611、rank_kind=cond`；`regime/agent.py:217-223` Hermes 正文能正确标出“同财报状态内分位(2年窗)”。 | `regime/agent.py:61-62` 的固定 `PANEL_LEGEND` 仍写“其分位是与自己历史比（252交易日窗）”。SQL 条件复算：在最近 504 行中标记未来 30 日是否有 earnings，排除当前行并保留相同状态 → `185 peers，17/185=0.091892`；最新 `iv=47.285`。前端 `web/app.js:773-776` 只显示“分位 0.09·财报窗内”，没有明确说它是条件分位。 | 更新固定图例为“优先 504 日同财报状态条件分位；样本不足回退 252 日原始分位”，前端同时显示 `rank_kind` 的中文标签。 | 高 |
| 3 | 不一致 | BTC 近端 IV=29.68、tenor=3.0d 能从库到前端和 collector，但 Hermes 丢掉 IV、tenor、method，仅留下 RV3 和剪刀差。 | `dashboard.py:237-249` 从 `opt_iv_near` 输出 `xopt.iv/tenor_days/method/n_expiries`；`dashboard.py:313-319` 放入加密 payload。`web/app.js:674-677` 显示“近端IV、~3.0d、插值、10到期”；`data/collector.log:124819` 显示 `BTC=29.7(~3.0d)`。 | SQL：`SELECT datetime(ts/1000,'unixepoch'),iv,tenor_days,method,n_expiries FROM opt_iv_near WHERE symbol='BTC-USDT' ORDER BY ts DESC LIMIT 1;` → `2026-08-05 06:16:34 / 29.68 / 3.0 / interp / 10`。`regime/agent.py:180-190` 的加密分支只输出 `DVOL/RV30/RV3/spread3`。零引用证明：`grep -n 'xopt' regime/agent.py` 仅返回 `266-267`，且属于商品/美股 `uv` 分支，没有 `dv.xopt` 消费者。 | 在加密 Hermes 文本中补 `近端IV、tenor、method、n_expiries、age`；否则 `3dIV−RV3` 缺少可审计的 IV 一侧。 | 高 |
| 4 | 干扰 | RV30/RV3 代码口径可复算且出口一致，但 README 仍描述升级前的“指数IV vs RV30 + 9D/3M”单卡，完全漏掉当前独立 RV3 卡。 | `regime/features/volatility.py:37-40` 定义 close-to-close 对数收益滚动标准差年化；`dashboard.py:634-644` 的美股 RV30=`30×1d、√365`；`dashboard.py:252-265` 的 RV3=`72×1h、√(24×365)`。SQL 复算：NVDA RV30=`48.461136%`；BTC RV3=`28.779479%`；NVDA RV3=`42.224347%`，分别与 payload `48.46/28.78/42.22` 一致。 | `web/index.html:68-74` 已分成“IV vs RV30 制度层”和“IV vs RV3 3d持仓前端”两卡；`web/app.js:708`、`web/app.js:758-760` 分别渲染 RV30/RV3；Hermes 在 `regime/agent.py:184-189,260,269-270` 同口径输出。反面：`README.md:74` 仍只写“指数IV vs 本品种RV30 + 9D/3M期限结构”。collector 没有 RV30/RV3 派生数值日志。 | README 改成现行四层架构，并明确 RV30 是 30 个日收益、RV3 是 72 个小时收益；不要只写容易与 ATR 混淆的 `RV30/RV3`。 | 高 |
| 5 | 不一致 | 当前代码和活体文案把 `IV−历史HV` 直接称作“方差风险溢价”，与仓内研究文档“IV−过去RV不是严格VRP”的结论相反。 | `dashboard.py:352-364` 实际计算同一 `stock_vol` 行的 `iv-hv`；SQL：`SELECT iv,hv,iv-hv FROM stock_vol WHERE symbol='NVDA-USDT' AND source='moomoo' ORDER BY ts DESC LIMIT 1;` → `47.285 / 42.092 / 5.193`，payload 为 `vrp=5.19`。`web/app.js:781,800-801` 和 `regime/agent.py:64-65,230-233` 均称其为 VRP/方差风险溢价。 | `docs/research/20260803/topic3-iv-vrp.md:43` 明确写：`IV−过去RV` 不是严格 VRP，严格定义涉及风险中性预期方差与物理测度未来方差预测。当前 Hermes 还在 `regime/agent.py:47-48` 泛称所有 `IV−RV` 为风险溢价，进一步混淆了加密 `DVOL−自算RV30` 与个股 `IV−厂商HV`。 | 活体出口改称“IV−历史HV剪刀差”或“VRP proxy”；只有实现匹配期限的方差口径后再使用严格 VRP 名称。 | 高 |
| 6 | 干扰 | 9D/30D 当前值与分位在 SQL、payload、前端、Hermes 中一致，但 README 仍把期限结构写成旧的单一 9D/3M。 | `dashboard.py:541-565` 分别计算 fast=`VIX9D/VIX`、slow=`VIX/VIX3M`，另保留 legacy `VIX9D/VIX3M`；payload 为 `term.fast/slow`。SQL 联结同日 `usvol` 并按 `usvol_csv_max_*` 截断：当前 fast=`0.911515`、3918 点、rank=`0.367534`；payload `0.912/0.368`。`web/app.js:807-810` 显示两段；`regime/agent.py:242-243` 同样输出两段。 | `README.md:74` 仍写“9D/3M期限结构（>1倒挂=近端恐慌）”，既漏掉 9D/30D 与 30D/3M 的双速设计，也把 legacy 比值当主口径。 | README 改为“双速：9D/30D 急性端、30D/3M 制度端”；将 `ts_ratio` 明标 legacy，评估前端兼容期结束后删除。 | 高 |
| 7 | 死代码 | `stock_option_stat` 中 780 日 NVDA put/call 历史链停在数据库，没有 dashboard payload、前端、Hermes 或只读查询提示消费者。 | SQL：`SELECT ts,pc_volume_ratio,pc_oi_ratio FROM stock_option_stat WHERE symbol='NVDA-USDT' AND source='moomoo' ORDER BY ts DESC LIMIT 1;` → `2026-08-04 / 0.449507 / NULL`；`regime/storage.py:672-699` 只有写入和 getter；`collector.py:326-329` 只负责持续采集。 | 零引用证明命令：`grep -RIn --exclude-dir=.git --exclude-dir=.venv --exclude-dir=.pytest_cache --exclude='*.bak' -E 'get_stock_option_stat|stock_option_stat' .`。输出摘要：生产调用仅 `collector.py:327-329` 自查水位及写入；其余为 storage、回填脚本、测试和文档；`dashboard.py/web/regime/agent.py` 零消费者。`regime/agent.py:458-459` 给 Hermes 的表清单甚至没有 `stock_option_stat`。 | 若这是刻意影子数据，至少加入只读查询白名单及明确“研究中、不进判定”的审计出口；否则按用户的数据保留意图裁决是否继续采集。 | 高 |
| 8 | 死代码 | 盘中 `stock_vol_live.pc_volume_ratio` 虽进入 `_live_iv_block` payload，但前端和 Hermes 均零引用，是确定的死 payload 键。 | `regime/moomoo_iv.py:120-133` 用 `put_volume/call_volume` 计算 live ratio；`dashboard.py:454-467` 输出 `iv.live.pc_volume_ratio`。SQL 最新 NVDA live 样本：`2026-08-04 19:55:04 / 0.454732`。 | 零引用命令：`grep -n -E 'pc_volume_ratio|pc_oi_ratio' web/app.js regime/agent.py vvvhermes.py main.py` → **零行输出**。全仓同词 grep 只命中生产者 `moomoo_iv.py`、payload 生成器 `dashboard.py:463-465`、schema/storage 与测试。 | 要么在 IV 实时区明确渲染“盘中 P/C 成交比”，要么从 live payload 删除该键，避免维护者误以为已有出口。 | 高 |
| 9 | 不一致 | OI 分位计算一致，但前端注明约 25.7 天参照窗，Hermes 在超过 21 天后完全隐藏窗口长度，使同一个 0.83 看起来像无期限限定的历史分位。 | `dashboard.py:780-817` 将 OI 统一到每小时末值并计算严格小于分位；SQL 按小时取末值复算 BTC 样本：`oi=108281.255，617小时点，rank=0.828201`，对应 payload 约 `0.83`。`web/app.js:870-875` 同时显示“历史 OI 25.7d”和分位。 | `regime/agent.py:280` 只输出 `OI=…(分位0.83)`；`regime/agent.py:287` 仅在 `warmup=True` 时加“历史<21天”，而 `dashboard.py:840` 一旦跨度≥21天就永久静默。`README.md:40` 明知 Binance OI 历史仅约30天。collector 如 `data/collector.log:125348` 只记录原始 OI，不记录分位窗。 | Hermes 始终输出 `spans.oi`，例如“近25.7日小时分位0.83”；前端 OI 标签也直接带窗口，避免用户漏看卡片 meta。 | 高 |
| 10 | 不一致 | margin 是按 raw_state 的规则树计算，却与迟滞后的 confirmed state 并排展示；当两态不同，方向文本会自相矛盾。 | `regime/classify.py:89-120` 的 `_boundary_margin(state,…)` 使用当次原始分类 state；`regime/classify.py:274-296` 将其写入审计快照。随后确认层会单独改变 `regime_history.state`。SQL：`SELECT state,raw_state,json_extract(features,'$.margin'),json_extract(features,'$.m_near') FROM regime_history WHERE symbol='CAT-USDT' AND tf='1h' ORDER BY ts DESC LIMIT 1;` → `trend_up / range / 0.02 / to_trend`。 | `dashboard.py:109-123` 取 confirmed state，却在 `dashboard.py:212-229` 返回重新按 raw classification 算出的 `features.margin`；`web/app.js:349-354,440-453` 只标“边界过渡”；Hermes `regime/agent.py:148-166` 实际形成“状态=趋势上行…原始=range…margin=0.02(to_trend)”的矛盾文本。 | payload 改名为 `raw_margin` 并明确“原始态→边界”；若需要 confirmed-state margin，应另按 confirmed state 重算，不能复用 raw margin。 | 高 |

## 自查盲区

- 未启动浏览器做像素级截图；前端结论来自 `build_dashboard()` 实际 payload、渲染源码及 Hermes 实际文本。
- collector 持续写库，OI、近端 IV 等活体值会有分钟级漂移；表中均给了采样时间，口径结论不受影响。
- 审查时 `stock_iv_term` 尚未经历首个 RTH、全表仍为 0 行，因此没有虚构 NVDA 3d/9d/30d 当前值。