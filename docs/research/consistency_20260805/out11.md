# 路线11：测试与代码的漂移

## 发现

| # | 类型(不一致/死代码/干扰) | 断言（一句话） | 证据A (file:line + 内容) | 证据B (file:line / grep / SQL) | 建议处置 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | 不一致 | `test_usvol_authority` 把周六当“新交易日”写入并断言保留，却在同一测试声明不得产生周末行。 | `tests/test_usvol_authority.py:25` 定义 `D01=2026-08-01`；`:68-86` 将其称为“当日/新交易日”，并断言该行值为 16.4、16.1。命令 `date -j -f '%Y-%m-%d' '2026-08-01' '+%A'` 输出 `Saturday`。 | `tests/test_usvol_authority.py:109-114` 又声明“不得造出周末行”，但只验证两个正常时间串；脏值只断言 `isinstance(date)`。`regime/usvol.py:53-65` 的脏值兜底确实返回 ET 当天，周末运行可能返回周末日期。只读 SQL：`SELECT COUNT(*) ... FROM usvol WHERE strftime('%w',ts/1000,'unixepoch') IN ('0','6')` 当前为 `0`，尚无真库污染。 | 将 D01 改为真实交易日；另用可控时钟钉住“脏时间串发生在周末时必须回退到上一交易日”，并在写入层拒绝非交易日。 | 高 |
| 2 | 不一致 | 符号翻转测试的名称/docstring 承诺“全程无 decoupled”，实际断言明确允许途经 decoupled。 | `tests/test_coupling_fsm.py:53-55`：`test_sign_reversal_is_not_decoupling`，称正确路径“全程无 decoupled”。 | `tests/test_coupling_fsm.py:60-63` 仅接受两类 reason 并检查最终回到 `coupled`；`:62` 注释反称“旧关系破裂途经 decoupled 是诚实记录”。没有任何 `not ... decoupled` 断言。 | 先确定现行语义：若允许旧关系进入 decoupled，就改测试名/docstring；若不允许，就增加事件序列的否定断言。 | 高 |
| 3 | 不一致 | 文件头宣称验证“稳态零误报”，但稳态测试只禁止最终级 `decoupled`，临时误入 `decoupling` 仍可全绿。 | `tests/test_coupling_fsm.py:1` 将“稳态零误报”列为测试主题。 | `tests/test_coupling_fsm.py:45-50` 只断言事件中没有 `to_state=="decoupled"` 且终态为 `coupled`。`regime/coupling_fsm.py:318-346` 明确把 `p_false_decoupling` 与 `p_false_decoupled` 都定义成误报率。 | 同时断言稳态样本没有进入 `decoupling`；如设计允许短暂预警，则把“零误报”改成“零确认脱耦”。 | 高 |
| 4 | 不一致 | `test_coupling.py` 文件头所称“EWMA 手算对照”并不存在，实际只是随机总体相关的宽容差冒烟。 | `tests/test_coupling.py:1`：`EWMA 手算对照`。 | `tests/test_coupling.py:18-27` 生成 4,000 个随机样本，只断言估计值距理论 0.6 小于 0.08；没有逐步手算的 EWMA 均值、方差、协方差期望值。 | 增加短序列、固定数值的逐步精确对拍；否则把文件头改为“总体相关收敛测试”。 | 高 |
| 5 | 干扰 | 财报条件分位测试的 docstring 仍描述“当日 IV=80、同组内居中”，实际 fixture 已改成 IV=70、同组最低、分位为 0。 | `tests/test_stock_vol.py:306-312` 称“今天（窗内，IV=80）……正确做法……应落在中间”。 | `tests/test_stock_vol.py:326-337` 实际追加 `iv=70.0`，并断言其低于所有窗内 80，`rank == 0.0`。 | 按当前 fixture 重写 docstring，明确这是“条件组最低值与全样本分位污染”的对拍。 | 高 |
| 6 | 干扰 | 实时 IV 预览测试中一条旧注释说无财报时分位应回退为 `None`，紧接着的现行断言和生产代码都要求回退全集并给出约 0.127。 | `tests/test_stock_vol.py:392`：`无财报记录 → ... 预览分位应回退为 None`。 | `tests/test_stock_vol.py:401-405` 断言 `rank_preview` 非空且在 0.10–0.15；`dashboard.py:450-453` 也明确用结算全集计算分位。 | 删除或改正 `:392` 的旧语义，避免后来维护者把正确的全集回退“修回” `None`。 | 高 |
| 7 | 干扰 | 两个半日市测试在找不到半日市时直接 `return`，会把目标断言静默变成通过。 | `tests/test_stock_vol.py:285-303` 的结算测试在 `half is None` 时 `return`；`:649-666` 的宽度槽测试采用同样结构。 | 命令 `grep -R -nE 'pytest\\.(skip|mark\\.skip)|^[[:space:]]*return' tests` 未找到显式 pytest skip，却定位到 `tests/test_stock_vol.py:298-299`、`:663-664` 两个测试内提前返回。 | 固定使用已知日期 `2026-11-27`，先断言其收盘为 13:00；找不到半日市应失败而非返回。 | 高 |
| 8 | 干扰 | 两个测试文件头仍分别称只“钉四件事/三件事”，实际已膨胀到 12/34 个用例，新增职责完全未入目录。 | `tests/test_backtest_causality.py:3-9` 只列四项；`tests/test_stock_vol.py:3-4` 只列三项。 | `grep -c '^def test_' tests/test_backtest_causality.py` 输出 `12`；对 `tests/test_stock_vol.py` 输出 `34`。后者实际还覆盖期权流、财报、VRP、期限结构、心跳和宽度槽等。 | 更新文件头为真实职责目录，或将 `test_stock_vol.py` 按存储、财报、期限结构、面板心跳拆分。 | 高 |
| 9 | 干扰 | `confirm_states` 文件头仍无条件写“一般态 2 根”，遗漏 v3.1 事件窗内 squeeze→趋势需要 3 根的现行例外。 | `tests/test_confirm_states.py:2-9` 自称“全分支回归”，并写“一般态 2 根、恢复 range 3 根、high_vol_chop 1 根”。 | `regime/classify.py:184-202` 对事件窗内 squeeze→趋势执行 `need += 1`；`tests/test_confirm_states.py:82-87` 自己也断言该路径需要 3 根。 | 在文件头写明事件门控例外，并将“确认根数”表述与 `_confirm_need` 的现行规则同步。 | 高 |
| 10 | 干扰 | `test_pathgeom` 已明确降格为确定性测试，但失败信息仍把相同输入对拍称作“因果性失败”。 | `tests/test_pathgeom.py:5-6`、`:35-39` 明确说明本文件只验证确定性，因果性由 `test_causality.py` 负责。 | `tests/test_pathgeom.py:46-50` 两侧输入值相同，断言消息仍为 `analyze 因果性失败`。 | 将错误信息改为“确定性失败”；避免失败时把问题误导到未来泄漏方向。 | 高 |
| 11 | 不一致 | 系统日志的“265 条 assert”是把三个 docstring 中的单词 `assert` 也算进去了；真实静态 assert 语句为 262 条。 | `SYSTEM_LOG_20260805.md:214`：`69 个用例 / 265 条 assert`。 | `grep -R -nE '\\bassert\\b' tests --include='*.py' \| wc -l` 为 `265`；`grep -R -nE '^[[:space:]]*assert\\b' ... \| wc -l` 为 `262`。多出的三行是 `tests/test_pathgeom.py:125`、`test_session_ds.py:165`、`test_usvol_authority.py:124` 的 docstring。 | 日志改为“262 个静态 assert 语句”；计数脚本改用 AST 或行首语句匹配。 | 高 |
| 12 | 死代码 | 测试目录存在四组已失去消费者的脚手架：未实例化类、未使用导入和无消费者累加器。 | `tests/test_coupling.py:51-52` 定义 `FakeConn`，实际 `:69` 传的是 `None`；`tests/test_coupling_fsm.py:11` 导入 `FSMParams`；`tests/test_stock_vol.py:11` 导入 `tempfile`；`tests/test_causality.py:80,84` 写入 `total`。 | 零引用证明：`grep -R -n -w FakeConn tests` 仅 1 行定义；`grep -R -n -w FSMParams tests` 仅 1 行导入；`grep -n -w tempfile tests/test_stock_vol.py` 仅 1 行；`grep -n -w total tests/test_causality.py` 仅初始化和 `+=`，之后无读取。 | 删除这些死脚手架；`FakeConn` 若想表达接口约束，应真正传给 `panel_returns`。 | 高 |
| 13 | 干扰 | 除已知 `upsert_ohlcv` 空白外，当前还有 12 个生产目标没有任何直接单测引用，现有“全绿”摘要没有呈现这层覆盖不均。 | 直接有测试目标共 16 个：`backtest/calendar_nyse/classify/coupling/coupling_fsm/data/pathgeom/volatility/vwap/storage/moomoo_iv/binance_opt_iv/stock_iv_term/usvol/collector/dashboard`；对应导入见 `tests/test_backtest_causality.py:19`、`test_calendar_nyse.py:8`、`test_causality.py:32`、`test_coupling.py:11`、`test_coupling_fsm.py:10`、`test_resample_4h.py:22`、`test_session_ds.py:23`、`test_stock_vol.py:16,438,575,654,673`、`test_usvol_authority.py:21-23`、`test_vwap.py:16`。 | 零直接目标：`regime/agent.py:71`、`breadth.py:43`、`deriv.py:32`、`experiments.py:54`、`features/crsi.py:46`、`structure.py:51`、`utils.py:8`、`volume.py:12`、`instruments.py:30`、`report.py:35`、`main.py:35`、`vvvhermes.py:42`。证明命令：`grep -R -nE 'from regime\\.(agent\|breadth\|deriv\|experiments\|instruments\|report)\|regime\\.features\\.(crsi\|structure\|utils\|volume)\|import (main\|vvvhermes)' tests --include='*.py'` 输出为空。部分模块会经 classify/dashboard 间接执行，故这里严格称“零直接单测”，不称零覆盖。 | 在测试治理文档增加“直接目标/间接覆盖/零覆盖”矩阵；优先补 `agent`、真实 `deriv` 数值清洗、`breadth.fetch_breadth`、CLI 白名单与 `report` 契约。 | 中 |

## 自查盲区

- 遵守只读要求，未实际运行 pytest；pytest 的缓存、字节码和临时目录行为可能产生写入。本路线依据静态逐行审查与只读 SQL。
- 未运行动态 coverage 或突变测试，因此“零直接单测”清单不等于“运行时绝对未经过”；已明确区分直接目标与间接装配覆盖。
- 未核查 GitHub Actions 的 68 次历史运行日志，只检查了当前测试源码和当前 CI 配置。