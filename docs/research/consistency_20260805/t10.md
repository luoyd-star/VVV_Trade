# 路线10：面板文案与 Hermes 上下文对后端口径
1. web/app.js 每一段用户可见中文文案（meta 行、图例、tooltip、徽章、折叠 summary）
   与它背后的 payload 字段语义逐条对——文案说"30d"数据是不是真 30d、说"结算"是不是真 settled、
   说"分位"用的哪个窗；
2. regime/agent.py 的 PANEL_LEGEND 与 system_brief：每个向模型解释的字段说明
   与 dashboard 实际注入的 <panel> 数字逐个对——缺解释的字段、解释错的字段、
   解释了但已不注入的字段（三个方向都要）；
3. scripts/vvvquery.py 的帮助文本/表清单与真实 schema 对。
