# 路线10：面板文案与 Hermes 上下文对后端口径

## 发现

| # | 类型(不一致/死代码/干扰) | 断言（一句话） | 证据A (file:line + 内容) | 证据B (file:line / grep / SQL) | 建议处置 | 置信(高/中/低) |
|---|---|---|---|---|---|---|
| 1 | 不一致 | 耦合面板把 `all247` 写成“加密+商品”，但实际还包含国际股永续 SKHY。 | `web/app.js:156`：`all247: '24/7（加密+商品）'`。 | `regime/coupling.py:43-57`：`intl_stock_perp` 明确归入 `all247`；`instruments.json:170-181`：SKHY 的 class 是 `intl_stock_perp`。实跑 `panel_members(...,'all247')` 返回列表含 `SKHY-USDT`。 | 改为“24/7（加密+商品+国际股永续）”，或由后端返回面板成员说明。 | 高 |
| 2 | 不一致 | 同一个可见符号 `°` 在矩阵表示“观察池”，在波动率卡却表示“未结算/未确权”，且页面没有可见图例解释任一语义。 | `web/app.js:219`：观察池品种名追加 `°`。 | `web/app.js:806-808`：`index_settled === false`、`term.fast.settled === false` 也追加 `°`；`dashboard.py:706` 定义其含义为盘中延迟报价、未确权。`grep -n '°' web/app.js web/index.html` 只有这四处，其中解释观察池的 `web/app.js:206` 只是源码注释。 | 使用不同标记，并在卡片及矩阵各加可见 tooltip/图例。 | 高 |
| 3 | 不一致 | 美股“持仓与杠杆”卡会把 moomoo 的长历史 IV 值/分位，配上 CBOE 影子列的短历史跨度。 | `web/app.js:870-884`：meta 显示 `sp.iv30` 天，指标区却显示 `dr.iv30`、`dr.iv30_rank`。 | `dashboard.py:573-586`：有 moomoo 时 `dr.iv30` 改取 `_stock_iv_block`；`dashboard.py:792-839`：`spans.iv30` 仍来自 `deriv.iv30`。SQL：`deriv.iv30` 对 QQQ 为 `4.6天/171点`，`stock_vol.moomoo` 为 `1135.0天/780点`。 | `iv30` 同时返回 `source/span/n/rank_kind`；meta 必须跟随最终选中的 source。 | 高 |
| 4 | 干扰 | 个股期限曲线一旦出数，UI/Hermes 会把“最近到期的实际天数”固定写成 3d/9d/30d，可能把同一到期日伪装成三个期限。 | `web/app.js:811-814` 和 `regime/agent.py:248-253`：固定显示 `3d/9d/30d`。 | `regime/stock_iv_term.py:25-36`：每个目标取最近到期，且明确允许不同目标落到同一到期；`dashboard.py:675-683` 已下发 `t3/t9/t30`，两个消费者均未使用。SQL：`SELECT COUNT(*) FROM stock_iv_term;` 当前为 `0`，故属首采后才触发的 dormant drift。 | 显示实际 `t3/t9/t30`；目标桶仅作为括号说明，例如“目标3d，实际5d”。 | 高 |
| 5 | 不一致 | 个股 IV 分位已经可能是 504 交易日的“同财报状态条件分位”，但 PANEL_LEGEND 仍统一解释为 252 日，前端多数时候也只写“分位”。 | `regime/agent.py:61-62`：称“分位…252交易日窗”；`web/app.js:773-776`：只有当前在财报窗内才加标记，窗外的条件分位仍只显示“分位”。 | `dashboard.py:343-351`：优先条件分位；`dashboard.py:471-477,504-514`：条件窗为 504 交易日，按未来30日有无财报分组，样本不足才回退252日。 | 所有消费面统一显示 `rank_kind` 和窗口，例如“同财报状态分位·504日”或“原始分位·252日”。 | 高 |
| 6 | 干扰 | PANEL_LEGEND 的迟滞规则说明漏掉 v3.1 的核心例外：财报事件窗内 squeeze→trend 要再加一根。 | `regime/agent.py:50-51`：只说明一般2根、恢复震荡3根、高波立即。 | `regime/classify.py:185-201`：未来10日有财报时，从 squeeze 进入 trend_up/down 的门槛 `need += 1`。 | 图例补充事件门槛及适用转换，最好由 `_confirm_need` 的结构化配置生成。 | 高 |
| 7 | 不一致 | SOL 等无 DVOL 的加密品种，前端能显示近端 IV，Hermes `<panel>` 却输出 `DVOL=None` 并完全丢掉近端 IV 数值和实际期限。 | `web/app.js:674-686`：读取 `d.xopt`，显示近端 IV、`tenor_days`、方法、到期数。 | `regime/agent.py:180-190`：只输出 `iv_last/iv_rank/RV/spread3`，未读 `dv.xopt`。SQL：SOL 最新 `opt_iv_near` 为 `44.48、3.0d、interp、3到期`；实跑 `vvvquery panel SOL-USDT` 输出 `DVOL隐含=None...3dIV−RV3=+10.4pt`，没有 44.48。 | Hermes 对无 DVOL 品种改写为“近端IV=44.48（实际3.0d）”；有 DVOL 时同时列制度层和前端层。 | 高 |
| 8 | 不一致 | 面板的五条分管线心跳没有进入 Hermes `<panel>`，所以“数据健康有问题吗？”无法覆盖 OpenD、期限曲线或近端 IV 静默断流。 | `dashboard.py:1003` 下发 `heartbeat`；`web/app.js:1059-1073` 显示正常/迟滞/断流/盘外及最后落库时间。 | `regime/agent.py:122-297` 的 `render_context` 未消费该字段。grep：`grep -n 'heartbeat' regime/agent.py` 输出 0 行。 | 在 `<panel>` 注入每条 lane 的 state、age、note；至少注入非 `ok/idle` 项。 | 高 |
| 9 | 不一致 | Hermes 无条件把个股 IV 称作“昨结算”，却丢弃后端已有的实际日期、年龄和陈旧标志。 | `regime/agent.py:219-224`：固定输出 `个股IV(昨结算)`。 | `dashboard.py:370-380` 下发 `asof/age_days/stale`；`web/app.js:787-800` 会在陈旧时显示 `⚠N天前`。grep `age_days\|stale` 于 `regime/agent.py` 为 0 行。 | 改成“最近结算 YYYY-MM-DD／N天前”，并把 `stale` 注入健康警告。 | 高 |
| 10 | 不一致 | Hermes 同样丢弃指数 IV 和期限结构的 `settled` 状态，盘中未确权报价会被模型当成普通正式数值。 | `dashboard.py:549-560,623-628`：指数和两条期限比均计算并下发 `settled`；前端至少以 `°` 标记。 | `regime/agent.py:242-243,258-259`：直接输出比率、分位和指数值，不读取 `settled/index_settled`。grep：`grep -nE 'index_settled|settled' regime/agent.py` 输出 0 行。 | 在文本中明确“盘中延迟报价／未确权”；未结算值不得用无修饰的正式口吻。 | 高 |
| 11 | 不一致 | `vvvquery overview` 给 Hermes 的 SQL 表清单只列8张，而真实库有19张业务表。 | `regime/agent.py:457-459`：表清单为 `ohlcv/regime_history/deriv/vol1h/usvol/dvol/stock_vol/ref_daily`。 | SQL：`SELECT COUNT(*), group_concat(name) ... FROM sqlite_schema WHERE type='table' AND name NOT LIKE 'sqlite_%';` → 19张：另有 `breadth/chat/earnings/live_bars/meta/opt_iv_near/stock_iv_term/stock_option_stat/stock_vol_live` 等。 | 从 `sqlite_schema` 动态生成清单，或明确写成“常用表举例”并提供 schema 子命令。 | 高 |
| 12 | 干扰 | `vvvquery states` 自称输出“状态+审计”，实际只投影5个审计特征，且完全不显示 rules/version/audit_version。 | `scripts/vvvquery.py:6`：示例注释“最近 N 行状态+审计”；`scripts/vvvquery.py:63-77`：只查询部分列，并只打印 `dir/er_rank/atr_rank/bbw_rank/warmup`。 | `regime/classify.py:274-309` 定义 a8 的25个审计键；SQL `COUNT(DISTINCT json_each.key)` → `25`。真实 `regime_history` 还含 `rules/version/audit_version`。 | 改称“状态+5项审计摘要”，或增加 `--full/--json` 输出完整审计记录。 | 中 |
| 13 | 死代码 | preview payload 的 `bar_ts`、`age_sec` 只有生产方，没有任何前端、Hermes、脚本或测试读取。 | `dashboard.py:190-197`：preview 中写入 `bar_ts` 和 `age_sec`。 | grep：`grep -RIn ... -E '"bar_ts"|"age_sec"' .` 仅命中 `dashboard.py:194,196`；再搜 `preview...bar_ts/age_sec` 输出 0 行。 | 删除两个字段；若本意是展示预览新鲜度，则补消费者和 tooltip 后再保留。 | 高 |
| 14 | 干扰 | 源码宣称“1h 翻转 Hermes/API 可查全量”，实际 API 最多40条，Hermes 注入最多6条。 | `web/app.js:1011-1012` 及 `web/index.html:93-94`：称“1h 不列，Hermes/API 可查全量”。 | `dashboard.py:851-867`：所有周期合并后仅返回 `flips[:40]`；`regime/agent.py:289-292`：Hermes 只注入 `flips[:6]`。 | 改成“可查最近记录”，或提供按 TF 分页的独立 flips 查询接口。 | 中 |
| 15 | 不一致 | 商品波动率虽然来自 GLD/SLV 代理，前端和 Hermes 的剪刀差仍标成“个股IV−RV”，把代理 ETF 说成商品自身个股 IV。 | `web/app.js:796` 能正确显示“结算IV(代理GLD)”，但 `web/app.js:803-804` 又按 `spread_src='stock'` 写“个股IV−RV”；`regime/agent.py:219-265` 同样生成“个股IV(昨结算)”和“个股IV−RV”。 | `instruments.json:378-390`：XAU class=`commodity`、vol_proxy=`GLD`；`dashboard.py:609,713-716`：proxy 存在但有 `iv` 即把 `spread_src` 设为 `stock`。SQL：XAU 当前该序列为 `moomoo 23.557`，即代理期权序列。 | 将来源枚举改为 `self/index/proxy`，商品统一显示“代理GLD IV／代理IV−RV”。 | 高 |

## 自查盲区

- 未启动浏览器做 ECharts 实际悬停与响应式布局核查；本次 tooltip/图例结论基于完整静态代码路径。
- `stock_iv_term` 当前为0行，因此第4项是确定的消费逻辑漂移，但尚无生产页面实例可截图复现。
- 未扩大到 `web/style.css` 的全部 CSS 类、ARIA 与 DOM 可达性专项；仅核查了路线10涉及的文案及 payload。