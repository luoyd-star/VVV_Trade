# 路线8：数据库死物与写读不对称
对 data/market.db（只读）+ storage.py + 全部调用方：
1. 每张表：谁写、谁读。写而零读的表/列（已知 bbo 与 universe_snapshot 整表，
   请精确到"哪些列即使在被读的表里也零消费"——如 ohlcv 的某列、deriv 七列逐列、
   stock_option_stat 各列、earnings 的 pub_type/period、opt_iv_near 的 n_expiries/index_price…）；
2. meta 表 338 键逐前缀分族：每族谁写谁读，死键清单（已知 regime_audit_* 5 个与
   deriv_backfilled v1 的 10 个，找新的；注意 stock_iv_term_last=0 这类"活键死值"）；
3. storage.py 里定义但零调用的存取函数；
4. 表里存了但从未进任何 payload/判定/实验的字段（"采而不用"清单——这不一定是错，
   但要有一张完整的账）。
