# 路线11：测试与代码的漂移
tests/ 12 个文件：
1. 每个测试 docstring 声称钉的不变量 vs 实际 assert 的东西——名不副实的测试；
2. 测试里引用的常量/行为是否已随重构改变而测试靠宽松断言"侥幸"绿着
   （如断言范围过宽、断言了旧语义但新语义恰好也满足）；
3. 测试文件头部注释里的过期描述；
4. 六个 test_all() 聚合文件里被注释掉或被条件跳过的断言；
5. 有单测的模块 vs 零单测的模块清单（对照 SYSTEM_LOG_20260805 的 P2-1，找它没列的）。
