# 路线13：重复实现与单一事实点violation
同一逻辑在两处独立实现（改一处忘一处的温床）：
已知 CRYPTO_DEFAULT vs DEFAULT_SOURCES 一例，系统性地找同类：
1. ET/时区转换逻辑出现在几处（calendar_nyse、moomoo_iv、stock_iv_term、collector、
   dashboard、experiments…）？语义是否一致（DST、日界）？
2. RTH/交易日判定的所有调用点是否都走 calendar_nyse，有没有旁路的硬编码时段判断；
3. pct_rank vs rolling_pct_rank vs 回测/实验里的分位计算——口径差异清单（NaN 处理、
   严格小于、窗长），哪些差异是刻意的哪些是漂移；
4. "未收线裁剪"（iloc[:-1] / settled_only / pk[:-1] / DVOL 末行）各处实现是否同语义；
5. 前端 fmtN/ago 等工具函数与后端格式化是否有重复且不一致的实现；
6. 品种过滤逻辑（美股类判定 class=='us_stock_perp'）散落几处、有无判据不一致
   （如有的地方把 intl_stock_perp 也算美股、有的不算）。
