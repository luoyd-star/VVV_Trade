# 路线12：治理件与脚本
scripts/ 全部 + weekly_rerun.sh + launchd/ + .github/workflows/ci.yml + requirements.txt +
.gitignore + instruments.json：
1. 每个脚本的头部说明 vs 实际行为；引用的表/列/函数是否还在；
2. instruments.json 的 _说明/_对账/_v2说明 三段元数据与 74 品种现实逐条对
   （对账数字、class 分布、sources 说明、vol_proxy 清单）；
3. weekly_rerun.sh 引用的脚本清单 vs scripts/ 现有内容（漏挂的、多余的）；
4. launchd plist 的参数 vs collector.py 当前 CLI（--once 还在吗、路径对吗）；
5. ci.yml 步骤 vs 本地测试现实；requirements 七条 vs 实际 import 面。
